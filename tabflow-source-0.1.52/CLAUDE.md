# TabFlow — Project Handoff for Claude

> **Note to Claude:** This document is the source of truth for continuing TabFlow development. Read this file first before touching anything. It describes what TabFlow is, how it's built, what's been done, and the rules you must follow. The human developer is **Sam** (shamilton@vortexgov.com). Update this document at the end of any meaningful work session.

---

## 1. What TabFlow Is

TabFlow is a Chrome MV3 browser extension that organizes tabs into **workspaces** (sometimes called "spaces"). Each workspace is a named container for a set of tabs, with its own color, icon, and metadata. The user's use case is separating different contexts (work, research, personal, projects) without drowning in one giant tab bar.

**Key product features:**

- Workspaces with custom names, colors, and optional short-name labels
- The new tab page (`chrome_url_overrides.newtab`) IS the app — it shows a sidebar of workspaces and a grid of tab tiles for the active workspace
- Drag-and-drop tab reordering and workspace reordering
- Per-tab memory stats and audible indicators
- Optional end-to-end encrypted cloud sync via the user's own self-hosted Supabase project
- Multi-device "Resume Working Here" model — only one device is actively pushing syncs at a time
- Workspace archive (recycle bin) — deleted workspaces can be restored with all their tabs
- Per-workspace tab history (snapshots) with restore
- Cross-device scroll position sync — scroll on one browser, pick up where you left off on another
- Tab thumbnails, search across all workspaces, native-messaging companion host

**Published as:** Unlisted extension on Chrome Web Store. Item ID: `gkcamehohljdpenmjmoaciigppdbjcgl`.

---

## 2. Repository Layout

This is an npm workspaces monorepo.

```
Browser Tab Manager Project/
├── CLAUDE.md                       ← this file
├── BEHAVIOR_SPEC.md                ← short behavioral spec (older)
├── ICON_GENERATION.md              ← how icons are built
├── architecture.html               ← older architecture notes
├── supabase-setup.sql              ← combined idempotent SQL for new users
├── package.json                    ← root workspace config
├── store-assets/                   ← Chrome Web Store assets + privacy policy
│   ├── icon-source.svg
│   ├── icon-{16,48,128}.png
│   ├── screenshot-{1,2,3}-*.png    (1280x800)
│   ├── store-listing.md
│   └── privacy-policy.html         ← hosted at https://samjham.github.io/TabFlow/store-assets/privacy-policy.html
└── packages/
    ├── core/                       ← @tabflow/core — platform-agnostic types, engine, crypto
    │   └── src/
    │       ├── models/types.ts     ← Workspace, Tab, Session, DeletedWorkspace, etc.
    │       ├── storage/StorageAdapter.ts
    │       ├── workspace/WorkspaceEngine.ts
    │       ├── sync/SyncClient.ts
    │       └── crypto/encryption.ts
    ├── browser-extension/          ← @tabflow/browser-extension — cross-browser MV3 extension (Chrome + Firefox)
    │   ├── public/
    │   │   ├── manifest.chrome.json   ← Chrome manifest template (copied in as manifest.json during build)
    │   │   ├── manifest.firefox.json  ← Firefox manifest template (gecko id, sidebar_action, etc.)
    │   │   └── icons/              ← copied into each dist at build time
    │   ├── vite.config.ts          ← TARGET env var picks the manifest and emits to dist/<target>/
    │   └── src/
    │       ├── background/         ← service worker, MessageHandler, TabManager
    │       ├── newtab/             ← main UI (NewTab.tsx + useWorkspaces hook)
    │       ├── popup/              ← toolbar popup
    │       ├── sidebar/            ← sidePanel (Chrome) / sidebarAction (Firefox) entry
    │       ├── storage/            ← IndexedDBAdapter (Dexie.js) — source of truth
    │       ├── sync/               ← SupabaseSyncClient
    │       ├── auth/               ← AuthManager + SetupWizard
    │       ├── components/         ← shared React components
    │       ├── content/            ← content scripts (youtube-time-tracker)
    │       ├── entries/            ← HTML entry points
    │       ├── browser-compat.ts   ← runtime/build-time browser detection + sidebar API wrapper
    │       └── config.ts           ← Supabase config loader (async from chrome.storage.local)
    ├── native-host/                ← optional native-messaging companion (install.bat for Chrome, install-firefox.bat for Firefox)
    └── supabase/                   ← SQL migrations + setup script
        ├── migrations/
        │   ├── 001_init.sql
        │   ├── 002_...
        │   ├── 003_...
        │   └── 004_active_devices.sql
        └── tabflow-setup.sql       ← combined idempotent script for onboarding
```

---

## 3. Absolute Rules (non-negotiable)

These are principles Sam has explicitly set. **Violating them causes real problems.**

### 3.1. The database is LOCKED DOWN

> *"The only time that 'database' changes is when I manually make a change."*

- IndexedDB (the local database) must **never** be mutated except in direct response to an explicit user action (clicking delete, dragging a tab, renaming a workspace, etc.) or a Chrome tab event that the app is explicitly tracking.
- Reads must be pure reads. `handleGetTabs` returns storage records AS-IS — it never deletes during a read, even if a tab looks stale.
- Sync is **push-only** from this device; pull from Supabase is currently disabled. Don't re-enable pull without explicit approval.
- Sync push is gated behind the active-device check (see §5.3). An inactive device must not push.

### 3.2. Be specific about file paths

Sam has explicitly asked: don't assume the user knows where things are. When instructing Sam to make a change or open something, give the **full path**. Example: say `C:\Users\shamilton\OneDrive - vortexgov\Documents\Claude\Browser Tab Manager Project` or `packages/browser-extension/src/newtab/NewTab.tsx`, not "the newtab file."

### 3.3. Storage is the source of truth, not Chrome

The model is: **IndexedDB is truth.** Tab event listeners update IndexedDB. The UI reads from IndexedDB. Chrome's own tab state is not trusted for persistence — it can be lost on restart. Do not design features that assume Chrome's in-memory tab state persists.

---

### 3.4. Test the Resume Working Here flow before any sync-touching release

> Added 2026-05-13 after a 0.1.13 release locked Sam out of his work PC.

Any change that modifies code under `packages/browser-extension/src/sync/`,
`packages/browser-extension/src/background/service-worker.ts` near the
auth/sync init, `NewTab.tsx`'s `isActiveDevice` / device-status polling,
`SupabaseSyncClient.ts`, or the `claimActiveDeviceWithMaterialization`
flow MUST be manually exercised end-to-end before the release ships:

1. Build the dev `.zip` and install temporarily on a second browser
   profile (or second machine) signed into the same Supabase project.
2. On Browser A (active device), make a small change like creating a
   workspace. Confirm it syncs.
3. Close Browser A entirely. Open Browser B.
4. Confirm: the Resume Working Here modal appears within 5 seconds.
5. Confirm: clicking Resume completes the claim and tabs materialize.
6. Confirm: the popup's "Force Claim This Device" button is visible.
7. If ANY step fails (modal hidden, claim stuck, popup button missing),
   the release is not ready. Don't ship.

The cost of two minutes of manual testing is far less than the cost of
locking Sam out of his work PC on a Monday morning. This rule is not
optional.


## 4. Key Technical Decisions

| Concern | Choice | Why |
|---|---|---|
| Storage (local) | IndexedDB via **Dexie.js** | Large capacity, async, indexes, schema versioning |
| Sync (cloud) | **Supabase**, self-hosted by each user | Avoids any vendor lock-in on Sam's side; users own their data |
| Encryption | AES-GCM via WebCrypto, derived from passphrase | Data at rest in Supabase is opaque to the Supabase host |
| Multi-device model | **"Resume Working Here"** — one active device at a time | Avoids merge conflicts; heartbeat-based claim with 2-min stale detection |
| UI framework | React 18 with inline `style={}` objects (no CSS framework) | Kept bundle small; each screen manages its own styles |
| Build | Vite (for extension), tsc (for core); `TARGET=chrome\|firefox` env var selects manifest and emits to `dist/<target>/` | Single source, two browsers |
| Entry point | The new tab page IS the app | `chrome_url_overrides.newtab` → newtab.html (works on both Chrome and Firefox) |
| New tab activation | User action only | Background never opens new tabs unprompted |
| Cross-browser | Single codebase targets Chrome + Firefox (MV3) | See §5.7 browser-compat shim |

---

## 5. Critical Subsystems

### 5.1. Message layer (background ↔ UI)

All UI → background communication goes through `chrome.runtime.sendMessage` with a typed `MessageType` enum in `packages/browser-extension/src/background/MessageHandler.ts`. When adding a feature:

1. Add a new `MessageType` enum value
2. Add a case in `handleMessage`'s switch
3. Implement a private `handle*` method
4. Add a method on `useWorkspaces` hook for the UI side
5. Wire the call site in React

### 5.2. Storage schema versions (Dexie)

`packages/browser-extension/src/storage/IndexedDBAdapter.ts`:

- **v1:** `workspaces`, `tabs`, `sessions`
- **v2:** Added `workspaceHistory` (per-workspace tab snapshots, 30-day retention)
- **v3:** Added `thumbnails` (cached page screenshots, LRU-evicted, max 500)
- **v4:** Added `deletedWorkspaces` (recycle bin, 90-day retention)

When adding a new table or index, **always add a new `this.version(N).stores({...})` block** — never mutate an existing version, or existing users' DBs will break.

### 5.3. Multi-device sync (Resume Working Here)

Table: `active_devices` on Supabase (migration 004). One row per user. Columns: `user_id` (PK), `device_id`, `device_name`, `claimed_at`, `last_heartbeat`.

Mental model: **the cloud is always the source of truth.** The active browser pushes constantly (every snapshot, every explicit action). When you open a different browser, it shows a blocking "Resume Working Here" modal — click it, it pulls from the cloud, nukes whatever was in that browser, recreates the workspaces and tabs to match, then claims as active and starts pushing.

**Absolute rule (added 2026-04-20):** the `active_devices` table is the **only** place that knows which device is active. No caches in `chrome.storage.session`, no caches in `chrome.storage.local`, no caches in in-memory variables used as authoritative sources. Every status read round-trips to Supabase via `SupabaseSyncClient.isActiveDeviceFromDb()`. This costs a handful of DB queries per minute per browser tab and eliminates an entire class of staleness bugs (Firefox missing a realtime notification while its service worker was suspended; Chrome reading a stale local flag after the service worker woke up; etc.). The in-memory `_isActiveDevice` flag still exists but is used only inside `SupabaseSyncClient` for heartbeat/realtime tight loops; every external read (UI polling, push gates, tab-lock enforcement) must use `isActiveDeviceFromDb()`.

Flow:
- On startup, `SupabaseSyncClient.initDeviceSession(deviceId)` subscribes to realtime changes and runs `checkActiveDevice()`.
- `checkActiveDevice()` auto-claims **only** if no device row exists. It will NOT auto-claim over another device's fresh row, and no longer tries to auto-claim over a stale row either — taking over requires the user to click "Resume Working Here."
- `startHeartbeat()` updates `last_heartbeat` every 30s while this device is active.
- `pushToSync()` and the snapshot-push in `snapshotActiveWorkspace` are both double-gated: `(await syncClient.isActiveDeviceFromDb()).isActive` AND `safeToPush` must both be true. `safeToPush` is set only after a successful claim or for migrated existing installs. The active-device check round-trips to Supabase on every push attempt — a device that has quietly lost the claim cannot push.
- When an inactive device wants to take over, the UI shows a blocking modal. Clicking it sends `CLAIM_ACTIVE_DEVICE`, which calls `claimActiveDeviceWithMaterialization()`: pull all from Supabase → close all browser tabs → open the active workspace's tabs → re-snapshot → claim on Supabase → enable push.
- The newtab page **polls** `GET_DEVICE_STATUS` — every 500ms until the first success, then every 3s — and updates the modal's visibility from the polled value. There is no `chrome.storage.session` broadcast and no `chrome.storage.onChanged` listener for device status. Polling + `isActiveDeviceFromDb()` is the only status channel.
- Realtime changes on `active_devices` trigger a re-query via `isActiveDeviceFromDb()` rather than trusting the payload — Supabase can deliver events out of order under load.

### 5.4. Onboarding (SetupWizard)

`packages/browser-extension/src/auth/SetupWizard.tsx`. Three paths:

1. **Skip (local-only):** Sets `tabflow_local_only: true` in chrome.storage.local. No Supabase at all.
2. **Already have Supabase account:** Jump to credential entry form → test connection → success.
3. **New Supabase user:** Numbered steps to create a project → SQL copy button (embedded `SETUP_SQL` constant) → credential entry → success.

`config.ts` has `isSupabaseConfigured()` which checks BOTH stored credentials AND existing `sb-*-auth-token` sessions — this prevents existing users from seeing the wizard on upgrade.

### 5.5. Workspace archive (recycle bin)

When a workspace is deleted (`handleDeleteWorkspace`), it first archives to the `deletedWorkspaces` IndexedDB table with the full workspace data + tab snapshot, THEN deletes from active tables. UI: the "Archive" section in the sidebar of `NewTab.tsx` expands to show a checkbox list with Restore and Delete buttons. Auto-pruned after 90 days.

### 5.6. Tab history (per-workspace snapshots)

Separate from the archive. On every meaningful tab change, the service worker takes a snapshot of the workspace's tabs (URL list dedup'd). Stored in `workspaceHistory`. UI: `showHistoryPanel` slider on NewTab lets the user scrub through history and restore. 30-day retention.

### 5.7. Cross-browser support (Chrome + Firefox)

The extension is built from a single source tree in `packages/browser-extension/` and targets both Chrome and Firefox via a build flag:

- `TARGET=chrome vite build` → `dist/chrome/` with `manifest.chrome.json` copied in as `manifest.json`
- `TARGET=firefox vite build` → `dist/firefox/` with `manifest.firefox.json` copied in as `manifest.json`

Picked in `packages/browser-extension/vite.config.ts` via the `postBuild` plugin. `publicDir` is left enabled (so `icons/` and `suspended.*` get copied) and the stray `manifest.<target>.json` templates are stripped out of the final dist by the same plugin.

Firefox manifest differences (`public/manifest.firefox.json`):
- Adds `browser_specific_settings.gecko.id` + `strict_min_version: 128.0` (FF 128 is when MV3 service_worker support landed)
- Removes Chrome-only permissions `sidePanel` and `system.memory`
- Replaces `side_panel` key with `sidebar_action`
- Everything else identical (including `chrome_url_overrides.newtab` — Firefox supports this, with a one-time user permission prompt)

Runtime compatibility is handled by `packages/browser-extension/src/browser-compat.ts`:
- Firefox exposes `chrome` as an alias for `browser`, so existing `chrome.*` calls mostly just work
- The few Chrome-only APIs (`chrome.sidePanel`, `chrome.system?.memory`) are already guarded with feature checks in `service-worker.ts`, so they no-op on Firefox
- `browserCompat.openSidebar(tabId?)` wraps `chrome.sidePanel.open()` / `browser.sidebarAction.open()` so UI code doesn't have to branch
- Vite's `define` injects `import.meta.env.TARGET_BROWSER` (`'chrome'` or `'firefox'`) so source code can check the build target

Native messaging (`packages/native-host/`) has separate installers because the registration differs:
- Chrome: `install.bat` → writes to `HKCU\Software\Google\Chrome\NativeMessagingHosts`, uses `allowed_origins: ["chrome-extension://<ID>/"]`
- Firefox: `install-firefox.bat` → writes to `HKCU\Software\Mozilla\NativeMessagingHosts`, uses `allowed_extensions: ["<gecko-id>"]`

### 5.8. Cross-device scroll position sync

Scroll position is synced across devices via Supabase as the **single source of truth** — no local cache, no IndexedDB middleman.

**Architecture:**
- Content script: `packages/browser-extension/src/content/scroll-tracker.ts` — runs at `document_start` on `<all_urls>`, registered in both manifests
- Disables `history.scrollRestoration = 'manual'` to prevent the browser fighting programmatic scroll restoration
- **CAPTURE:** Listens for `scroll` (debounced 2s), `visibilitychange` (flush on tab hidden), and `beforeunload`. Sends `SAVE_SCROLL_POSITION` to the service worker
- **RESTORE:** On page load AND on tab re-activation (visible after >3s hidden), sends `GET_SCROLL_POSITION` to the service worker
- Restore uses aggressive retry schedule (150/400/800/1500/2500/4000/6000ms) for lazy-loaded content, with a 7s timeout
- User input (wheel, touchstart, scroll-keys) cancels restore immediately

**Service worker handlers:**
- `SAVE_SCROLL_POSITION`: Finds the matching tab record by canonical URL in the active workspace → pushes scroll_x/scroll_y directly to Supabase via `syncClient.pushTab()`. Skips the `isActiveDevice` gate (scroll updates are safe from any device — they only update an existing row)
- `GET_SCROLL_POSITION`: Finds the matching tab record → queries Supabase directly via `syncClient.getTabScroll(tabId)` → returns the result. No cache, no IndexedDB — Supabase every time

**Critical design rule:** Supabase is the only place scroll position is read from or written to. Earlier iterations used an in-memory cache and IndexedDB as intermediaries, which caused persistent staleness bugs where each browser returned its own last-known position instead of the cross-device value. The fix was removing all middlemen.

**Supabase columns:** `scroll_x` and `scroll_y` on the `tabs` table (integers, default 0). The `pushTab` method's `excludeScroll` option (used by snapshot pushes, workspace switches, and `fullSyncPush`) omits these columns from the upsert so batch operations don't overwrite individually-pushed scroll values.

---

### 5.9. Architectural decision: hidden windows on startup AND browser close

The startup reconciler closes ALL non-main windows. This handles three
cases at once:
  1. Leftover hidden workspace windows from a previous session (Firefox
     session-restored them but we don't preserve PIP across browser restart)
  2. Stray windows opened by Firefox features like browser.startup.homepage
  3. Any user-opened windows from before browser restart

PIP is preserved across workspace switches (within a session) via the
hidden-window mechanism, but ONLY when at least one outgoing tab has active
PIP — detected by the pip-tracker content script. Most workspace switches
create no hidden window.

When the user closes the last visible browser window, the
chrome.windows.onRemoved listener closes any remaining hidden workspace
windows so the browser process actually exits (instead of staying alive as
a background process with hidden tool-windows).

This is a deliberate change from the earlier policy (where hidden windows
were preserved on startup). The user clarified they don't care about PIP
surviving browser close. The bug fixed by this change: Firefox stays alive
as a background process due to leftover hidden tool-windows, then on
"reopen" no startup events fire and the reconciler doesn't run.


## 6. New Machine Setup

Follow these steps when setting up the project on a fresh computer (e.g. home PC).

**Prerequisites — install these first if you don't have them:**

1. **Git:** Download from https://git-scm.com/downloads — use default options during install. After install, open a terminal and run `git --version` to confirm.
2. **Node.js (LTS):** Download from https://nodejs.org — pick the LTS version. After install, confirm with `node --version` and `npm --version`.
3. **Configure your Git identity** (one-time, so commits have your name):
   ```
   git config --global user.name "Sam Hamilton"
   git config --global user.email "shamilton@vortexgov.com"
   ```

**Clone and build:**

1. Open a terminal and `cd` to wherever you want the project folder to live (e.g. `cd C:\Users\YourUser\Documents`).
2. Clone the repo:
   ```
   git clone https://github.com/samjham/tabflow.git
   ```
3. Enter the folder and install dependencies:
   ```
   cd tabflow
   npm install
   ```
4. Build the Chrome extension:
   ```
   npm run build:chrome
   ```

**Load the extension in Chrome:**

1. Go to `chrome://extensions`
2. Toggle **Developer mode** on (top right)
3. Click **Load unpacked**
4. Select the `packages/browser-extension/dist/chrome` folder inside the cloned repo

**Set up native host (optional — needed for taskbar-hiding and memory stats):**

1. Open a terminal in the project folder
2. Run:
   ```
   cd packages/native-host
   install-host.bat
   ```
3. Restart Chrome

**Day-to-day workflow — pulling changes from your other machine:**

Before starting work, pull the latest:
```
git pull origin main
```

After making changes, push them:
```
git add -A
```
```
git commit -m "describe what you changed"
```
```
git push origin main
```

Then on the other machine, `git pull origin main` to get those changes.

---

## 7. Build & Run

```bash
# Install
npm install

# Build both browsers (produces dist/chrome and dist/firefox)
npm run build

# Build one target
npm run build:chrome
npm run build:firefox

# Dev mode (defaults to chrome target; for Firefox use build:firefox + `web-ext run`)
npm run dev:chrome

# Typecheck
npm run typecheck

# Tests
npm run test
npm run test:smoke  # puppeteer-based end-to-end
```

To load unpacked in Chrome:

1. `chrome://extensions`
2. Toggle Developer mode on (top right)
3. Click **Load unpacked**
4. Select `packages/browser-extension/dist/chrome`

To load temporarily in Firefox (does not persist across restarts):

1. Open `about:debugging#/runtime/this-firefox`
2. Click **Load Temporary Add-on…**
3. Select `packages/browser-extension/dist/firefox/manifest.json`

To build all release zips in one step:

```bash
npm run release
```

This runs `npm run build` then `node scripts/package-release.mjs`, producing
four zips at the project root, all tagged with the current manifest version:

- `tabflow-chrome-<VERSION>.zip` — dev build. Includes the `key` field
  from `manifest.chrome.json` so unpacked installs get the pinned
  extension ID `gkcamehohljdpenmjmoaciigppdbjcgl`. Use this to load
  unpacked in Chrome.
- `tabflow-chrome-<VERSION>-store.zip` — Chrome Web Store upload.
  Same contents, but with the `key` field stripped from the manifest.
  Required because the store has its own public key on file from the
  first submission (2026-04-12, before the `key` field existed). Uploads
  that include a `key` are rejected with "key field value in the
  manifest doesn't match the current item".
- `tabflow-firefox-<VERSION>.zip` — AMO upload. No `key` field in
  Firefox manifests.
- `tabflow-source-<VERSION>.zip` — AMO source requirement (Mozilla
  demands source for any extension shipping minified code). Ships the
  repo minus `node_modules/`, `dist/`, `.git/`, `bin/`, `obj/`, Vite
  temp files, and previously-built release zips. Includes
  `BUILD_FROM_SOURCE.md` at the repo root with reviewer-focused build
  steps.

The packaging script (`scripts/package-release.mjs`) handles all of this;
you should rarely need to touch it directly. If you do want to run only
the packaging step without rebuilding (e.g. after tweaking the script),
use `npm run package`.

---

## 8. Publishing state

- **Chrome Web Store** submission: UNLISTED. Version 0.1.52 ready to submit — five improvements: (1) Resume Working Here modal grace period bumped from 4s to 8s so slow cold-starts don't briefly flash the modal, (2) massive sync-egress reduction — SWITCH_WORKSPACE / CREATE_WORKSPACE / MOVE_TABS in pushToSync no longer push every tab across every workspace (they now push only workspace rows plus the destination workspace's tabs; snapshotActiveWorkspace's normal push handles the rest), (3) heartbeat now updates device_name so Firefox rows no longer show as "Chrome on Windows", (4) pip-tracker content script + pipActiveTabs Set + PIP_STATE_CHANGED / QUERY_PIP_STATE plumbing all removed (dead code since 0.1.46's persistent-tab checkbox superseded auto-PIP-detection), (5) Force Push / Force Pull nuclear-option buttons added to the popup. 0.1.51 shipped two follow-up fixes from Sam's 0.1.50 diagnostic: (1) 4-second Resume Working Here modal grace period so the modal doesn't flash on every SW restart while sync init is still resolving `isActiveDevice` from 'unknown', and (2) broader startup sweep of stale `moved-*` / `dup-*` placeholder tab IDs across ALL workspaces (0.1.50's sweep only ran on the active workspace; Sam's diagnostic showed `moved-17` still lingering in five inactive workspaces). The IIFE now iterates every workspace via `workspaceEngine.getWorkspaces(LOCAL_USER_ID)` and calls `tabManager.sweepStalePlaceholderIds` on each, logging total renamed + deduped counts. 0.1.50 shipped four fixes derived from Sam's 0.1.49 diagnostic session: (1) loud detection + banner for PostgREST schema-cache-missing-column errors so silent sync failures become visible with the exact ALTER TABLE + NOTIFY SQL, (2) single-active-workspace invariant enforcement in `saveWorkspace` (atomic Dexie transaction clears isActive on siblings) plus a startup-reconciler repair for the multi-active state Sam hit, (3) diagnostic report URL matching now unwraps `suspended.html?url=` before canonicalizing so the Active Workspace Detail section stops reporting false 0-matching / 13-missing / 13-extra divergences, (4) stale `moved-*` / `dup-*` placeholder tab IDs are now regenerated to proper deterministic `tab-<hash>` IDs (or deduped if a proper record already exists) — both via the startup reconciler and inside `saveCurrentTabsToWorkspace`'s cleanup sweep. 0.1.49 shipped comprehensive sync-flow instrumentation for diagnosing Resume Working Here bugs. Adds comprehensive logging to `claimActiveDeviceWithMaterialization` (every step), extends `SupabaseSyncClient` with an `onDiagnostic` callback that routes push/pull/claim/state-check events into the SW diagnostic buffer, logs active-device state transitions with previous vs new, and compares cloud vs local active-workspace on SW startup. The Diagnose report gained "Recent Claim Events" and "Recent Sync Trace" sections. Also dedupes the double-reconcile on browser restart: `chrome.runtime.onStartup` now checks whether the IIFE already ran the reconciler in the last 15 seconds and skips if so. 0.1.46 introduced — introduces user-controlled tab preservation across workspace switches. New per-tab checkbox marks a tab as "keep alive"; on workspace switch, checked tabs on Firefox are hidden via `chrome.tabs.hide()` (added `tabHide` permission) and unhidden on switch back, preserving PIP videos, form state, audio. Chrome build shows the checkbox disabled with a tooltip explaining Chrome doesn't support tab hiding. New `persistent` column on Supabase `tabs` table — **existing users MUST re-run `packages/supabase/tabflow-setup.sql` on their Supabase project or the flag silently fails to sync**. The 0.1.38 PIP-aware `moveTabsToHiddenWindow` code path is no longer called; user's explicit flag is the sole signal. New `hiddenTabs` map in `chrome.storage.session` tracks storageId → chromeTabId for hidden persistent tabs. `chrome.tabs.onRemoved` prunes stale entries; `chrome.tabs.onUpdated` safety net re-hides tabs Firefox force-shows. 0.1.45 added the visible "Diagnose" button on the TabFlow newtab page that gathers a comprehensive plaintext report of extension state, browser state, workspaces, sync status, and the last 500 diagnostic log entries, then copies it to clipboard in one click. New `GET_DIAGNOSTIC_REPORT` message handler in the SW builds the report via a `buildDiagnosticReport()` helper. Every section is wrapped in try/catch so one broken query doesn't kill the report. No user secrets included (IDs truncated to 8 chars). The 0.1.44 Ctrl+Shift+D Debug panel stays as an advanced real-time view. 0.1.44 shipped the in-app diagnostic log viewer (Ctrl+Shift+D on the new-tab page): rolling 500-entry buffer persisted in chrome.storage.local, recorded from critical code paths (startup, reconcile, PIP, workspace-switch, sync). Instrumentation only — no operational logic changes. 0.1.42 was two surgical fixes. **(1)** PIP-tracker content script's `QUERY_PIP_STATE` handler (added in 0.1.40) was returning `true` after a synchronous `sendResponse()`, breaking the live PIP-state query. The SW's awaited `chrome.tabs.sendMessage` may not receive the response when the channel is left open after a sync reply — depends on browser/version. The fallback then returned no-PIP, so workspace switches closed PIP source tabs instead of moving them to a hidden window, killing PIP instantly. Fix: removed the `return true` line. **(2)** On browser startup, `reconcileToActiveWorkspace`'s final `backfillThumbnails` step sometimes left a workspace tab active rather than the TabFlow newtab. When TabFlow isn't visible, the browser throttles JS, React doesn't mount, device-status polling doesn't fire, and the Resume Working Here modal never appears — user has to click `+` to manually open TabFlow. Fix: at the very end of `reconcileToActiveWorkspace` (after backfill), explicitly `chrome.tabs.update(tabFlowTabId, { active: true })`. 0.1.41 fixed PIP being killed on every service worker wake. 0.1.38 had added a startup-reconciler step that closes all non-main windows to clean up orphans, but MV3 SWs terminate when idle and restart on any event; the IIFE ran the reconciler every wake. So after a workspace switch put PIP-having tabs into a hidden window, the very next SW wake closed that window and killed the video. Fix: `reconcileToActiveWorkspace` now reads the `hiddenWindows` map from chrome.storage.local first; windows whose IDs are in the map are intentional (workspace tabs preserved for PIP) and skipped — only true orphan windows (not main, not in map) get closed. After the close pass, the map is pruned of any IDs that no longer exist (catches stale entries from before this fix and from real browser restart, where Firefox reassigns window IDs). PIP preservation now survives SW sleep/wake within a session; PIP is still lost across browser restart (acceptable, by design). 0.1.40 was the (now-superseded) attempt to fix the same user-visible symptom by persisting `pipActiveTabs` to chrome.storage.session and adding a `QUERY_PIP_STATE` live-query content script — that work is kept (it's still useful for the workspace-switch decision) but it was orthogonal to the actual bug, which was the reconciler killing the hidden window unconditionally. 0.1.39 fixed the "Sync not initialized" error race in Resume Working Here. The 0.1.38 PIP-aware switch used an in-memory `Set<number>` that died when the MV3 service worker went idle; now persisted to `chrome.storage.session` with a `chrome.tabs.sendMessage`-based live-query fallback via the `pip-tracker` content script. 0.1.39 fixed the "Sync not initialized" error race in Resume Working Here. `CLAIM_ACTIVE_DEVICE`, `RESTORE_FROM_CLOUD`, and `claimActiveDeviceWithMaterialization` now wait up to 5 seconds for the SW's async sync init before erroring. `NewTab.tsx` clears `claimError` when `isActiveDevice` becomes `'active'` and suppresses the error display while `claimInProgress` is true or the device is already active. 0.1.38 was PIP-aware hidden windows + browser-close cleanup. Fixes the bug where Firefox stayed alive as a background process after the user closed it, because hidden workspace windows (created on every workspace switch in 0.1.37) prevented the process from exiting. New `pip-tracker` content script reports Picture-in-Picture state to the SW; workspace switches now only create a hidden window when at least one outgoing tab has active PIP. New `chrome.windows.onRemoved` listener closes leftover hidden windows when the last visible browser window closes. Startup reconciler closes all non-main windows (cleans up any leftover hidden windows from 0.1.37 upgrades). Trade-off accepted: PIP no longer survives browser restart (user confirmed they don't care). 0.1.37 was cross-device sync gap closure: workspace history, deleted-workspaces archive, workspace shortName, and sidebar-width-on-Resume now sync via Supabase. **Existing users MUST re-run `packages/supabase/tabflow-setup.sql` on their Supabase project to pick up the new tables and column** — without that, the new history/archive pushes return 42P01 "relation does not exist" and those features silently stop syncing (local-only fallback still works). Three new schema additions: `workspace_history` table (encrypted JSONB tab snapshots, 30-day retention), `deleted_workspaces` table (encrypted JSONB of the workspace + tabs at deletion time, 90-day retention), and a new `short_name TEXT` column on `workspaces`. `SupabaseSyncClient` gained `pushHistoryEntry`/`pullHistory`/`pruneOldHistory`, `pushDeletedWorkspace`/`pushRestoredWorkspace`/`pullDeletedWorkspaces`/`pruneOldDeletedWorkspaces`, and shortName now round-trips through the new column (the old local-only `shortNameMap` preservation hack in `pullAll` is removed). Service worker pushes history alongside each snapshot save, pushes to/removes from the cloud archive via two new MessageHandler callbacks, and `claimActiveDeviceWithMaterialization` now also pulls history, archive, and preferences (sidebar width) so Device B's Resume Working Here produces full state including history scrubbing. 0.1.36 was three Firefox startup bugfixes. (1) The IIFE no longer fires `reconcileToActiveWorkspace` after a fixed 200ms delay; a new `waitForTabsToSettle()` helper tracks the timestamp of the last `chrome.tabs.onCreated` event and waits until 1.5s have passed with no new tab creations (min 500ms floor, max 8s ceiling). Firefox session restore takes 500-800ms (sometimes more) and was firing onCreated events AFTER the reconciler closed everything and materialized workspace tabs — the user saw stray session-restored tabs like a Puget Systems homepage. The settle-based wait runs the reconciler against the final tab state. (2) `MessageHandler.handleRestoreHistoryEntry`'s tabs-to-close filter switched from `!ct.pinned && ct.url !== 'chrome://newtab/'` (which closes the TabFlow tab if Firefox session restore un-pinned it, since the actual newtab URL is `chrome-extension://<id>/newtab.html` or `moz-extension://<id>/newtab.html`) to a positive-protection filter that excludes the tab matching the stored `tabFlowTabId` AND any tab whose URL starts with `getExtensionPageUrl('newtab.html')`. The TabFlow tab is now protected regardless of pinned state. (3) `ensureTabFlowTab` gained a unified pin-verification step at the bottom — after picking a keeper (whether via stored ID, URL search, or fresh create), it calls `chrome.tabs.get(id)` to read the live pinned state and `chrome.tabs.update(id, { pinned: true })` if the keeper isn't pinned. Closes the gap where the "stored ID matched" branch returned without explicitly re-pinning if the query result reported `pinned: true` but Firefox had subsequently un-pinned it. Heavy console.log coverage on all three paths so users can share SW output if anything still goes wrong. 0.1.35 was the content-script operation overlay covering ANY active tab during system operations. New content script `operation-overlay.ts` runs on `<all_urls>` at `document_start`, polls the SW's `GET_OPERATION_STATUS` every 250ms, and injects/removes a full-screen blocking overlay (z-index `2147483647`, dark backdrop + blur + centered card + spinner) on whatever page is currently active. Skips itself on TabFlow's own newtab page where the React modal already covers. Fixes the confusing flicker users saw during the post-switch thumbnail backfill — when backfill briefly activated a workspace tab to capture its thumbnail, the React modal on the newtab wasn't visible and the user saw the raw underlying page. Now the overlay follows focus, so the loading state is always visible. 0.1.34 was the blocking loading modal + thumbnail backfill moved inside the workspace switch operation. The post-switch tab-activation race is gone: backfill no longer fires-and-forgets via `setTimeout(..., 1000)` after the switch returns; it now runs INSIDE `runSystemOperation`, after `finalReconcile`, while the gate is still set. User clicks during the (~10–30s) backfill can't interleave with the backfill's tab activations because the UI's loading overlay was simultaneously upgraded from a thin top banner (`pointer-events: none`) to a centered full-screen blocking modal styled like the Resume Working Here modal — large spinner, title ("Switching workspace…"), subtitle ("Please wait — TabFlow is updating your tabs."), dark backdrop with blur, `pointer-events: auto`. Same treatment for `reconcileToActiveWorkspace` (startup) and `claimActiveDeviceWithMaterialization` (Resume Working Here): each materializes the workspace, runs `finalReconcile`, runs `backfillThumbnails` inline, then returns. `SystemOperationGate`'s max-lifetime auto-clear was bumped from 60s to 5 minutes so the gate doesn't auto-clear during a long backfill (the modal is blocking, so a long wait is acceptable). Per-tab backfill time tightened from ~500ms to ~400ms. The `onScheduleBackfillThumbnails` MessageHandler callback was renamed to `onPostOperationBackfill` and changed from `void`-returning fire-and-forget to `Promise<void>` so the operation awaits backfill before clearing the gate. The `backfillThumbnails` entry guard (`isSwitchingWorkspaces || isStartingUp || isGateSetSync()`) was removed — those would always be true during inline use; cancellation is now strictly via the per-call token and detection of the user activating a different tab in the browser's native tab strip. The `isBackfillingThumbnails` flag still exists harmlessly as belt-and-suspenders. 0.1.33 was the DB-as-truth startup refactor. Replaced the destructive `runChromeRestartFlow` (and all its detection logic — session markers, version comparison, startup-completed flag, Step 3 safety check) with a single non-destructive `reconcileToActiveWorkspace` function. Algorithm: find/create TabFlow tab via the 0.1.32 URL search, un-minimize the main window if needed, close every non-TabFlow tab in the main window, materialize the active workspace's tabs from DB via `restoreWorkspaceTabs`, then `saveCurrentTabsToWorkspace` to sync DB back. Wrapped in `runSystemOperation('startupReconcile', ...)` and called from both the IIFE and `chrome.runtime.onStartup` — idempotent. Hidden workspace windows for INACTIVE workspaces are deliberately left alone (preserves PIP-survives-restart). 0.1.32 was the Firefox install/restart "extra window" bug fix at the actual root. Three coordinated changes. (1) `ensureTabFlowTab` searches by URL across all windows before creating, and only creates a new tab if no TabFlow tab exists anywhere; when it does create, it uses `chrome.windows.getLastFocused` to pick the target window rather than `chrome.tabs.query({})[0].windowId` (which on Firefox could land in a hidden/minimized previous-session window). (2) `runChromeRestartFlow` Step 3 verifies the cached `tabFlowWindowId` is still present in `chrome.windows.getAll()` before closing other windows, aborting the entire flow with an error log if not — prevents the worst-case "close every window because our cached ID is wrong" scenario. (3) Persistent `tabflow_startup_completed` flag in `chrome.storage.local`: set after any successful startup path (extension reload or restart flow); cleared by `chrome.runtime.onStartup` (real browser restart). The IIFE and `ensureStartupComplete` both check it before running their restart-detection branches, so startup runs at most once per actual browser session. 0.1.31 was the thumbnail reliability overhaul. Two coordinated parts. **Part 1: capture pipeline hardened.** TabManager now caches `chromeTabId → storageId` for the active workspace as a byproduct of `saveCurrentTabsToWorkspace`'s position-matching; `captureTabThumbnail` looks up the storage ID directly via `tabManager.getStorageIdForTab(tabId)` instead of redoing position-matching at capture time (eliminates a class of silent failures when multiple tabs share a URL). The 1500ms debounce on activation-triggered captures was removed — captures fire immediately when the user clicks a tab. Page-load-complete captures keep a 300ms debounce to let layout settle. The `thumbnailCaptureInProgress` set is now keyed by Chrome tab ID instead of URL so same-URL tabs don't block each other. Capture errors that aren't permission/tab-gone get one retry after 500ms. The newtab page polls `GET_THUMBNAILS` every 1s for the first 30s after the active workspace changes, then settles to every 5s. **Part 2: bulk thumbnail backfill.** One second after a workspace switch completes (gate clear, finalReconcile done), a background pass iterates the new workspace's tabs, briefly activating each one that lacks a thumbnail, capturing, and moving on. ~250ms per tab. Eligibility filter: must be loaded, not at a suspended.html URL, not at a privileged URL. The pass restores focus to the originally-active tab when done. New module-level flag `isBackfillingThumbnails` keeps the bulk activations from triggering snapshot writes or workspace switches; tab event listeners check it alongside `isSwitchingWorkspaces` and `isGateSetSync()`. Cancellation: the pass aborts if the user starts a workspace switch, opens a system operation, or activates a different tab manually. An incrementing token guards against double-running. 0.1.30 was Phase 3 of the gate-hardening refactor: added a UI loading overlay during system operations. New `MessageType.GET_OPERATION_STATUS` exposes the read-only state of the `SystemOperationGate`. `NewTab.tsx` polls every 300ms and renders a small floating banner at the top of the page (with a CSS-only spinner and a friendly label — "Switching workspace…", "Restoring from history…", "Loading from cloud…", "Moving tabs…", "Restoring session…") whenever an operation is in progress. The banner is `pointer-events: none`, so YouTube videos keep playing during a workspace switch and the user can still click anywhere on the page. Workspace sidebar items also visually dim and reject clicks while an operation is active — the gate already drops queued clicks at the SW level, but visual feedback prevents the "did my click register?" confusion. New helper `getGateStateRaw()` exported from `SystemOperationGate.ts` mirrors `isGateSet`'s max-lifetime auto-clear logic so the UI can never see a stuck gate as "in progress" forever. 0.1.29 was two follow-up bugfixes after 0.1.28: (1) PIP pop-back auto-switch — when the user popped a Picture-in-Picture video back into its source tab on Firefox while the source tab lived in a hidden (minimized) workspace window, Firefox un-minimized the source window and the user landed in a window TabFlow still thought was inactive. Added a `chrome.windows.onFocusChanged` listener in `service-worker.ts` that detects external focus on any known hidden-workspace window and routes through the canonical `SWITCH_WORKSPACE` handler, gated by the same `isStartingUp`/`isSwitchingWorkspaces`/`isGateSetSync()` checks the tab event listeners use so the listener never auto-switches during an in-progress system operation. New `TabManager.getWorkspaceForHiddenWindow(windowId)` does the reverse lookup. (2) Version line restored in the new-tab page's header — the 2026-04-23 changelog claimed this had shipped in 0.1.7 but the gating clause on the memory block (`systemMemory.total > 0`) meant the line never rendered when the native host wasn't installed; added `TABFLOW_VERSION = chrome.runtime.getManifest().version` and `BROWSER_LABEL = isFirefox ? 'Firefox' : 'Chrome'` constants, always render the memoryBlock, swapped the hardcoded "Chrome" → `BROWSER_LABEL`, and added the `TabFlow v<VERSION>` line as the third row. 0.1.28 was Phase 2 of the gate-hardening refactor (see REFACTOR_PLAN.md): added a `finalReconcile(workspaceId, operationName)` helper called at the end of each system operation, before the SystemOperationGate clears. It settles 200ms then re-snapshots the active workspace to the DB, catching tabs the operation opened that hadn't fully registered when the main body returned, plus any user actions during the operation (which the gate suppressed). All 5 system operations now end with `finalReconcile`: `handleSwitchWorkspace` reconciles the destination workspace (replaces an explicit `saveCurrentTabsToWorkspace`), `handleRestoreHistoryEntry` reconciles the restored workspace when active, `claimActiveDeviceWithMaterialization` replaces the old re-snapshot, `handleMoveTabsInServiceWorker` reconciles the source (active) workspace where tabs were closed, `runChromeRestartFlow` reconciles the active workspace after Step 6's tab restoration. Also removed the 0.1.21 "preserve records created within last 5 seconds" rule from `saveCurrentTabsToWorkspace` — Phase 1's robust gate prevents snapshots from firing during operations, so by the time a snapshot runs all freshly-created tabs are already in chrome.tabs.query and cleanup can trust the live window as truth. The `preservedPending` (in-flight) and `preservedInert` (cross-browser privileged) rules stay; they protect against unrelated cases. 0.1.27 was Phase 1 of the gate-hardening refactor (see REFACTOR_PLAN.md): introduced a new `SystemOperationGate` module that persists "operation in progress" state across SW restart via chrome.storage.session. All five system operations (`handleSwitchWorkspace`, `handleRestoreHistoryEntry`, `claimActiveDeviceWithMaterialization`, `handleMoveTabsInServiceWorker`, `runChromeRestartFlow`) now run inside a `runSystemOperation(name, fn)` wrapper that uses try/finally to guarantee the gate clears even on exception, and a 60s max-lifetime auto-clear in case anything forgets to clear. Tab event listeners check the persisted gate via `isGateSetSync()` in addition to the in-memory `isSwitchingWorkspaces` flag, so an SW death mid-operation no longer leaves snapshots running against half-written state. Critical bug from the audit: `handleMoveTabsInServiceWorker`'s nested setTimeout-based gate management was leak-prone (gate could stick ON if SW crashed between the inner timeouts); refactored to use proper async/await within the wrapper. 0.1.26 reverted 0.1.24's verify-and-retry block in `restoreWorkspaceTabs`. That block was tightening a symptom (incomplete restoration) by creating duplicates when `chrome.tabs.create` resolved before the tab's URL settled — the verify check saw the tab as missing and created another one. Sam observed this as duplicated AI workspace tabs on restore and double-copies when moving a tab between workspaces. Removing the retry stops the duplication; restoration unreliability remains until Phase 1 fixes the gate's leaks. 0.1.25 adds version-comparison detection alongside the session marker so Firefox extension auto-update (which can wipe `chrome.storage.session`) no longer slips past the in-session-restart check and triggers `runChromeRestartFlow`. The IIFE writes `lastSeenVersion` to `chrome.storage.local` (which persists across update) and compares it against `chrome.runtime.getManifest().version`; a mismatch means an extension update just happened, so we skip Chrome-restart detection. Belt-and-suspenders with the session marker — either signal triggers the skip branch. 0.1.24 extended 0.1.23's session-marker protection to `ensureStartupComplete` (which had its own stale-tabFlowTabId detection that triggered the destructive flow on the very next message after install) and adds a verify-and-retry loop to `restoreWorkspaceTabs` to catch `chrome.tabs.create` calls that resolve without the resulting tab actually appearing in `chrome.tabs.query`. 0.1.23 fixed the actual root cause of install-causes-data-loss after 0.1.22 was insufficient. The IIFE has a separate Chrome-restart detection path that fires `runChromeRestartFlow` (whose Step 3 closes all non-TabFlow windows). Detection heuristic: if `tabFlowTabId` doesn't resolve to a real tab, treat as Chrome restart. Firefox extension auto-update reassigns numeric tab IDs even though the browser itself didn't restart, so the stored ID was stale and the cleanup flow ran. 0.1.23 adds a `chrome.storage.session` marker that persists across SW reload but is cleared by the browser on real restart; if present, the IIFE skips Chrome-restart detection entirely. 0.1.22 removed the `closeAllHiddenWindows()` call from the IIFE that destroyed all inactive workspace tabs at the browser level on every SW reload. Firefox preserves all browser windows across extension reload, so hidden workspace windows (with their tabs intact) persist; the next workspace switch finds them via `restoreTabsFromHiddenWindow` and moves them back to the main window with full state preserved (video playback, scroll, forms). No re-creation, no chrome.tabs.create race, no data loss. Added `TabManager.rehideInactiveWorkspaceWindows()` called 500ms after native host reconnects to re-apply WS_EX_TOOLWINDOW on the persisted hidden windows. 0.1.21 fixes a timing bug in `saveCurrentTabsToWorkspace`'s cleanup sweep that destroyed freshly-restored tabs. When `restoreWorkspaceTabs` opens many tabs via sequential `chrome.tabs.create` calls, tab registration is asynchronous and `chrome.tabs.query` may not see them all in time. The snapshot fired, cleanup saw "orphans," and deleted records the user just asked to restore. Fix preserves any record with `createdAt`/`updatedAt` within the last 5 seconds. Covers all restore paths (workspace switch, history restore, claim materialization, extension reload) uniformly. 0.1.20 switched thumbnail storage from URL-keyed to tab-ID-keyed, fixing the bug where multiple tabs sharing a URL (e.g., several YouTube homepage tabs) all displayed the same thumbnail because captureVisibleTab overwrote a single shared `thumbnails[url]` entry. Capture path now matches the chrome tab to its storage record by canonical URL + position-among-same-URL and stores under the deterministic storage ID; GET_THUMBNAILS handler accepts tabIds; NewTab.tsx renders by tab.id. 0.1.19 removed 0.1.17's `restoreWorkspaceTabs` dedup (the 0.1.18 strict cloud mirror makes it unnecessary, and the band-aid was destroying legitimate user-created duplicates) and adds the long-claimed-but-never-shipped `isSwitchingWorkspaces` gate around `MessageHandler.handleRestoreHistoryEntry` so history-restore tabs no longer get silently deleted mid-restore by racing snapshot events. 0.1.18 made cloud sync a strict mirror of the active device's local state for the active workspace — makes cloud sync a strict mirror of the active device's local state for the active workspace. `snapshotActiveWorkspace`'s cloud push now queries Supabase for the workspace's tab IDs and deletes any not present locally, eliminating accumulated orphan rows that the upsert-only model couldn't clean up. Sam's existing dirty cloud data self-cleans on the first snapshot after install. 0.1.17 fixed duplicate tabs appearing in workspaces after Resume Working Here by deduping by canonical URL in `restoreWorkspaceTabs` before opening browser tabs; one Resume cycle on that version self-cleaned both local IndexedDB and Supabase. 0.1.16 fixed a real regression where the new-tab page's device-status check was one-shot on mount with no recurring polling, so a device that quietly lost its active claim would never learn about it until the page was reloaded. Adds a 3-second recurring poll plus a visibilitychange/focus immediate re-poll. 0.1.15 Previous 0.1.14 hardened the Resume Working Here flow with a popup "Force Claim This Device" button, tri-state device status, and a 5-second safety timeout. 0.1.13 was the partial hover-expand bounce fix. 0.1.12 ships sidebar wrap + drag-resize + hover-expand + cross-device width sync. 0.1.11 parallelized the snapshot push (YouTube `&t=` resume bug). 0.1.10 added Data API GRANT statements for post-May-30-2026 Supabase onboarding. 0.1.9 brought workspace-switch un-minimize. 0.1.8 fixed Firefox restart leaving workspace tabs inside a minimized hidden window. 0.1.7 brought PIP-survives-workspace-switch and the version-number UI tweak.
- **Firefox Add-ons (AMO):** Version 0.1.52 ready to submit — five improvements: grace-period bump (4s→8s), sync egress reduction on workspace switch, heartbeat updates device_name, pip-tracker code removal, and Force Push/Pull buttons in the popup. Firefox especially benefits from #3 (device_name) since Sam's Firefox row was showing as "Chrome on Windows", and from the sync egress reduction (Firefox tends to snapshot more often when switching between many workspaces). See Chrome entry above for full details. 0.1.51 shipped two follow-up fixes from Sam's 0.1.50 diagnostic (see Chrome entry above): 4-second Resume modal grace period after mount to avoid flashing the modal while sync init resolves `isActiveDevice`, and a broader startup sweep of stale placeholder IDs across ALL workspaces (0.1.50 only swept the active one). 0.1.50 shipped four fixes derived from Sam's 0.1.49 diagnostic (see Chrome entry above): schema-cache banner, single-active-workspace invariant, diagnostic-report URL matching unwrap, and moved-*/dup-* placeholder cleanup. 0.1.49 shipped comprehensive sync-flow instrumentation. Adds comprehensive logging to `claimActiveDeviceWithMaterialization`, an `onDiagnostic` callback in `SupabaseSyncClient` for push/pull/claim events, active-device state-change logging, cloud-vs-local active-workspace comparison on startup, and two new report sections ("Recent Claim Events", "Recent Sync Trace"). Dedupes double-reconcile on browser restart by having `chrome.runtime.onStartup` skip when the IIFE ran within the last 15 seconds. 0.1.46 introduced — introduces user-controlled tab preservation across workspace switches (the marquee Firefox-specific feature — Chrome can't do this because `chrome.tabs.hide()` doesn't exist there). New per-tab checkbox on each tile marks a tab as "keep alive" — on switch away, checked tabs are hidden via `chrome.tabs.hide()` (new `tabHide` manifest permission) and restored via `chrome.tabs.show()` on switch back. Preserves PIP, forms, audio. **Existing users MUST re-run `tabflow-setup.sql`** on their Supabase project to pick up the new `persistent` column; otherwise the flag silently fails to sync. Removes the 0.1.38 PIP-aware `moveTabsToHiddenWindow` path entirely — user's flag is the sole signal for preservation. 0.1.45 added the visible "Diagnose" button on the TabFlow newtab page that gathers a comprehensive plaintext report and copies it to clipboard in one click. See the Chrome entry above for details. The 0.1.44 Ctrl+Shift+D Debug panel stays as an advanced real-time view. 0.1.44 shipped the in-app diagnostic log viewer (Ctrl+Shift+D on the new-tab page): rolling 500-entry buffer persisted in chrome.storage.local, recorded from critical code paths (startup, reconcile, PIP, workspace-switch, sync). Instrumentation only — no operational logic changes. 0.1.42 was two surgical fixes. (1) PIP-tracker's `QUERY_PIP_STATE` handler no longer returns `true` after a sync `sendResponse()` (kept the channel half-open and broke the live PIP query, causing workspace switches to kill PIP). (2) `reconcileToActiveWorkspace` now ends with an explicit `chrome.tabs.update(tabFlowTabId, { active: true })` so the TabFlow newtab is active on startup — React mounts, device-status polling fires, the Resume modal appears without user intervention. 0.1.41 fixed PIP being killed on every service worker wake. 0.1.38's startup-reconciler step closed all non-main windows on every SW wake (MV3 SWs terminate when idle and restart on any event), which killed the hidden window holding PIP-having tabs and so killed the video. Fix: `reconcileToActiveWorkspace` reads the `hiddenWindows` map first and skips windows whose IDs are in it; only true orphan windows are closed. The map is pruned after the close pass. PIP is preserved across SW sleep/wake within a session; still lost across browser restart, by design. 0.1.40 persisted `pipActiveTabs` to chrome.storage.session and added a live-query content script — kept but orthogonal to this bug. 0.1.39 fixed the "Sync not initialized" error race in Resume Working Here. The 0.1.38 PIP-aware switch used an in-memory `Set<number>` that died when the MV3 service worker went idle; now persisted to `chrome.storage.session` with a `chrome.tabs.sendMessage`-based live-query fallback via the `pip-tracker` content script. 0.1.39 fixed the "Sync not initialized" error race in Resume Working Here. `CLAIM_ACTIVE_DEVICE`, `RESTORE_FROM_CLOUD`, and `claimActiveDeviceWithMaterialization` now wait up to 5 seconds for the SW's async sync init before erroring. `NewTab.tsx` clears `claimError` when `isActiveDevice` becomes `'active'` and suppresses the error display while `claimInProgress` is true or the device is already active. 0.1.38 was PIP-aware hidden windows + browser-close cleanup. Fixes the bug where Firefox stayed alive as a background process after the user closed it, because hidden workspace windows (created on every workspace switch in 0.1.37) prevented the process from exiting. New `pip-tracker` content script reports Picture-in-Picture state to the SW; workspace switches now only create a hidden window when at least one outgoing tab has active PIP. New `chrome.windows.onRemoved` listener closes leftover hidden windows when the last visible browser window closes. Startup reconciler closes all non-main windows. 0.1.37 was cross-device sync gap closure (workspace history, deleted-workspaces archive, workspace shortName, sidebar-width-on-Resume). Existing self-hosted-Supabase users MUST re-run `tabflow-setup.sql` to add the two new tables and one new column; otherwise the new pushes silently fail with 42P01. See the Chrome entry above for the full breakdown. 0.1.36 was three Firefox startup bugfixes. (1) IIFE waits for tab events to settle (1.5s with no `chrome.tabs.onCreated` events, min 500ms / max 8s) before running `reconcileToActiveWorkspace`. The old fixed 200ms wait fired before Firefox finished session restore, so session-restored tabs slipped past the reconciler and showed as stray tabs after startup. (2) `handleRestoreHistoryEntry` no longer closes the TabFlow tab when it's unpinned — the filter now protects the stored `tabFlowTabId` plus any tab whose URL starts with `getExtensionPageUrl('newtab.html')`. (3) `ensureTabFlowTab` adds a unified pin-verification step at every return path, calling `chrome.tabs.update(id, { pinned: true })` whenever the live tab isn't pinned. 0.1.35 was the content-script operation overlay covering ANY active tab during system operations. New content script `operation-overlay.ts` runs on `<all_urls>` at `document_start`, polls the SW's `GET_OPERATION_STATUS` every 250ms, and injects/removes a full-screen blocking overlay (z-index `2147483647`, dark backdrop + blur + centered card + spinner) on whatever page is currently active. Skips itself on TabFlow's own newtab page where the React modal already covers. Fixes the confusing flicker users saw during the post-switch thumbnail backfill — when backfill briefly activated a workspace tab to capture its thumbnail, the React modal on the newtab wasn't visible and the user saw the raw underlying page. Now the overlay follows focus, so the loading state is always visible. 0.1.34 was the blocking loading modal + thumbnail backfill moved inside the workspace switch operation. The post-switch tab-activation race is gone: backfill no longer fires-and-forgets via `setTimeout(..., 1000)` after the switch returns; it now runs INSIDE `runSystemOperation`, after `finalReconcile`, while the gate is still set. The UI's loading overlay was upgraded from a thin top banner (`pointer-events: none`) to a centered full-screen blocking modal styled like the Resume Working Here modal — large spinner, title, subtitle, dark backdrop with blur, blocks clicks. Same treatment for `reconcileToActiveWorkspace` (startup) and `claimActiveDeviceWithMaterialization` (Resume Working Here). `SystemOperationGate` max-lifetime bumped from 60s to 5 minutes to cover long backfills. Per-tab backfill tightened from ~500ms to ~400ms. 0.1.33 was the DB-as-truth startup refactor. Replaced the destructive `runChromeRestartFlow` (and all its detection logic — session markers, version comparison, startup-completed flag, Step 3 safety check) with a single non-destructive `reconcileToActiveWorkspace` function. Algorithm: find/create TabFlow tab, un-minimize the main window if needed, close every non-TabFlow tab in the main window, materialize the active workspace's tabs from DB via `restoreWorkspaceTabs`, then `saveCurrentTabsToWorkspace`. Wrapped in `runSystemOperation('startupReconcile', ...)`. Hidden workspace windows for INACTIVE workspaces are deliberately left alone (preserves PIP-survives-restart). 0.1.32 was the Firefox install/restart "extra window" bug fix at the actual root. Three coordinated changes. (1) `ensureTabFlowTab` searches by URL across all windows before creating, and only creates a new tab if no TabFlow tab exists anywhere; when it does create, it uses `chrome.windows.getLastFocused` to pick the target window rather than `chrome.tabs.query({})[0].windowId` (which on Firefox can land in a hidden/minimized previous-session window). (2) `runChromeRestartFlow` Step 3 verifies the cached `tabFlowWindowId` is still present in `chrome.windows.getAll()` before closing other windows, aborting the flow with an error log if not. (3) Persistent `tabflow_startup_completed` flag in `chrome.storage.local`: set after any successful startup path; cleared by `chrome.runtime.onStartup`. The IIFE and `ensureStartupComplete` both check it before running their restart-detection branches. 0.1.31 was the thumbnail reliability overhaul. **Part 1: capture pipeline hardened.** TabManager caches `chromeTabId → storageId` from each `saveCurrentTabsToWorkspace`; `captureTabThumbnail` does a direct lookup instead of position-matching. Activation-triggered captures fire immediately (no 1500ms debounce); page-load-complete captures keep a 300ms debounce. In-progress dedup is now keyed by Chrome tab ID, not URL. Transient capture errors retry once after 500ms. UI polls `GET_THUMBNAILS` every 1s for the first 30s after the active workspace changes, then every 5s. **Part 2: bulk thumbnail backfill.** ~1s after a workspace switch (gate clear, finalReconcile done), a background pass briefly activates each thumbnail-less, loaded, http(s) tab at ~250ms cadence, captures, and restores focus to the originally-active tab. New `isBackfillingThumbnails` flag suppresses snapshot writes during the pass; tab event listeners check it alongside the other system-operation flags. Pass aborts on user interaction (workspace switch, manual tab activation) and on supersession by a newer backfill. 0.1.30 was Phase 3 of the gate-hardening refactor: added a UI loading overlay during system operations. New `MessageType.GET_OPERATION_STATUS` exposes the read-only state of the `SystemOperationGate`. `NewTab.tsx` polls every 300ms and renders a small floating banner at the top of the page (with a CSS-only spinner and a friendly label) whenever an operation is in progress. The banner is `pointer-events: none`, so YouTube videos keep playing during a workspace switch. Workspace sidebar items also visually dim and reject clicks while an operation is active. 0.1.29 was two follow-up bugfixes after 0.1.28: (1) PIP pop-back auto-switch — when the user popped a Picture-in-Picture video back into its source tab on Firefox while the source tab lived in a hidden (minimized) workspace window, Firefox un-minimized the source window and the user landed in a window TabFlow still thought was inactive. Added a `chrome.windows.onFocusChanged` listener in `service-worker.ts` that detects external focus on any known hidden-workspace window and routes through the canonical `SWITCH_WORKSPACE` handler, gated by the same `isStartingUp`/`isSwitchingWorkspaces`/`isGateSetSync()` checks the tab event listeners use so the listener never auto-switches during an in-progress system operation. New `TabManager.getWorkspaceForHiddenWindow(windowId)` does the reverse lookup. (2) Version line restored in the new-tab page's header — the 2026-04-23 changelog claimed this had shipped in 0.1.7 but the gating clause on the memory block (`systemMemory.total > 0`) meant the line never rendered when the native host wasn't installed; added `TABFLOW_VERSION = chrome.runtime.getManifest().version` and `BROWSER_LABEL = isFirefox ? 'Firefox' : 'Chrome'` constants, always render the memoryBlock, swapped the hardcoded "Chrome" → `BROWSER_LABEL`, and added the `TabFlow v<VERSION>` line as the third row. 0.1.28 was Phase 2 of the gate-hardening refactor (see REFACTOR_PLAN.md): added a `finalReconcile(workspaceId, operationName)` helper called at the end of each system operation, before the SystemOperationGate clears. It settles 200ms then re-snapshots the active workspace to the DB, catching tabs the operation opened that hadn't fully registered when the main body returned, plus any user actions during the operation (which the gate suppressed). All 5 system operations now end with `finalReconcile`: `handleSwitchWorkspace` reconciles the destination workspace (replaces an explicit `saveCurrentTabsToWorkspace`), `handleRestoreHistoryEntry` reconciles the restored workspace when active, `claimActiveDeviceWithMaterialization` replaces the old re-snapshot, `handleMoveTabsInServiceWorker` reconciles the source (active) workspace where tabs were closed, `runChromeRestartFlow` reconciles the active workspace after Step 6's tab restoration. Also removed the 0.1.21 "preserve records created within last 5 seconds" rule from `saveCurrentTabsToWorkspace` — Phase 1's robust gate prevents snapshots from firing during operations, so by the time a snapshot runs all freshly-created tabs are already in chrome.tabs.query and cleanup can trust the live window as truth. The `preservedPending` and `preservedInert` rules stay. 0.1.27 was Phase 1 of the gate-hardening refactor (see REFACTOR_PLAN.md): introduced a new `SystemOperationGate` module that persists "operation in progress" state across SW restart via chrome.storage.session. All five system operations (`handleSwitchWorkspace`, `handleRestoreHistoryEntry`, `claimActiveDeviceWithMaterialization`, `handleMoveTabsInServiceWorker`, `runChromeRestartFlow`) now run inside a `runSystemOperation(name, fn)` wrapper that uses try/finally to guarantee the gate clears even on exception, and a 60s max-lifetime auto-clear in case anything forgets to clear. Tab event listeners check the persisted gate via `isGateSetSync()` in addition to the in-memory `isSwitchingWorkspaces` flag, so an SW death mid-operation no longer leaves snapshots running against half-written state. Critical bug from the audit: `handleMoveTabsInServiceWorker`'s nested setTimeout-based gate management was leak-prone (gate could stick ON if SW crashed between the inner timeouts); refactored to use proper async/await within the wrapper. 0.1.26 reverted 0.1.24's verify-and-retry block in `restoreWorkspaceTabs`. That block was tightening a symptom (incomplete restoration) by creating duplicates when `chrome.tabs.create` resolved before the tab's URL settled — the verify check saw the tab as missing and created another one. Sam observed this as duplicated AI workspace tabs on restore and double-copies when moving a tab between workspaces. Removing the retry stops the duplication; restoration unreliability remains until Phase 1 fixes the gate's leaks. 0.1.25 adds version-comparison detection alongside the session marker so Firefox extension auto-update (which can wipe `chrome.storage.session`) no longer slips past the in-session-restart check and triggers `runChromeRestartFlow`. The IIFE writes `lastSeenVersion` to `chrome.storage.local` (which persists across update) and compares it against `chrome.runtime.getManifest().version`; a mismatch means an extension update just happened, so we skip Chrome-restart detection. Belt-and-suspenders with the session marker — either signal triggers the skip branch. 0.1.24 extended 0.1.23's session-marker protection to `ensureStartupComplete` (which had its own stale-tabFlowTabId detection that triggered the destructive flow on the very next message after install) and adds a verify-and-retry loop to `restoreWorkspaceTabs` to catch `chrome.tabs.create` calls that resolve without the resulting tab actually appearing in `chrome.tabs.query`. 0.1.23 fixed the actual root cause of install-causes-data-loss after 0.1.22 was insufficient. The IIFE has a separate Chrome-restart detection path that fires `runChromeRestartFlow` (whose Step 3 closes all non-TabFlow windows). Detection heuristic: if `tabFlowTabId` doesn't resolve to a real tab, treat as Chrome restart. Firefox extension auto-update reassigns numeric tab IDs even though the browser itself didn't restart, so the stored ID was stale and the cleanup flow ran. 0.1.23 adds a `chrome.storage.session` marker that persists across SW reload but is cleared by the browser on real restart; if present, the IIFE skips Chrome-restart detection entirely. 0.1.22 removed the `closeAllHiddenWindows()` call from the IIFE that destroyed all inactive workspace tabs at the browser level on every SW reload. Firefox preserves all browser windows across extension reload, so hidden workspace windows (with their tabs intact) persist; the next workspace switch finds them via `restoreTabsFromHiddenWindow` and moves them back to the main window with full state preserved (video playback, scroll, forms). No re-creation, no chrome.tabs.create race, no data loss. Added `TabManager.rehideInactiveWorkspaceWindows()` called 500ms after native host reconnects to re-apply WS_EX_TOOLWINDOW on the persisted hidden windows. 0.1.21 fixes a timing bug in `saveCurrentTabsToWorkspace`'s cleanup sweep that destroyed freshly-restored tabs. When `restoreWorkspaceTabs` opens many tabs via sequential `chrome.tabs.create` calls, tab registration is asynchronous and `chrome.tabs.query` may not see them all in time. The snapshot fired, cleanup saw "orphans," and deleted records the user just asked to restore. Fix preserves any record with `createdAt`/`updatedAt` within the last 5 seconds. Covers all restore paths (workspace switch, history restore, claim materialization, extension reload) uniformly. 0.1.20 switched thumbnail storage from URL-keyed to tab-ID-keyed, fixing the bug where multiple tabs sharing a URL all displayed the same thumbnail. Capture path matches the chrome tab to its storage record by canonical URL + position-among-same-URL and stores under the deterministic storage ID; GET_THUMBNAILS handler accepts tabIds; NewTab.tsx renders by tab.id. 0.1.19 removed 0.1.17's `restoreWorkspaceTabs` dedup (the 0.1.18 strict cloud mirror makes it unnecessary, and the band-aid was destroying legitimate user-created duplicates) and adds the long-claimed-but-never-shipped `isSwitchingWorkspaces` gate around `MessageHandler.handleRestoreHistoryEntry` so history-restore tabs no longer get silently deleted mid-restore by racing snapshot events. 0.1.18 made cloud sync a strict mirror of the active device's local state for the active workspace — makes cloud sync a strict mirror of the active device's local state for the active workspace. `snapshotActiveWorkspace`'s cloud push now queries Supabase for the workspace's tab IDs and deletes any not present locally, so orphan rows accumulated from past bugs/races no longer persist. Sam's existing dirty cloud data self-cleans on the first snapshot after install. 0.1.17 deduped by canonical URL in `restoreWorkspaceTabs` before opening browser tabs, so Resume Working Here no longer materialized duplicate rows from a contaminated cloud; one Resume cycle self-cleaned both local and cloud data. 0.1.16 added recurring 3-second device-status polling + visibilitychange/focus re-poll in the new-tab page (the 2026-04-20 DB-only refactor removed the broadcast but never actually added the replacement poll). 0.1.15 was the deeper hover-expand bounce fix. 0.1.14 hardened Resume Working Here with the popup Force Claim button, tri-state device status, and 5s safety timeout. 0.1.13 was the partial hover-expand bounce fix. 0.1.12 brought sidebar wrap/drag-resize/hover-expand and cross-device width sync. 0.1.11 parallelized snapshot push. 0.1.10 added Data API GRANTs. 0.1.9 brought workspace-switch un-minimize. 0.1.8 fixed Firefox restart hidden window.
- **GitHub:** https://github.com/samjham/TabFlow (public, used for hosting the privacy policy via GitHub Pages)
- **Privacy policy URL:** https://samjham.github.io/TabFlow/store-assets/privacy-policy.html
- **Extension ID (Chrome):** `gkcamehohljdpenmjmoaciigppdbjcgl`
- **Gecko ID (Firefox):** `tabflow@samhamilton.dev`

When bumping the extension version in `manifest.json`, also bump in `package.json` if you care (not strictly required — Chrome Web Store only reads manifest.json).

---

## 9. Known issues / deferred items

- **Broad host permissions warning** from Chrome Web Store review: TabFlow uses `<all_urls>` because it genuinely needs to read every tab's URL to organize them AND the scroll-tracker content script runs on all pages. Listed as a warning, not a blocker, but reviews take longer.

---

## 10. How to update this document

At the end of any meaningful work session (new feature, bug fix, publishing event), update:

- **§ 5.** if you added a new subsystem
- **§ 5.2** if you added a Dexie schema version
- **§ 8.** if publishing state changed
- **§ 9.** if you discovered a new known issue OR resolved a listed one
- **§ 11.** changelog below — add a dated entry summarizing what changed

Keep this document tight. It's for quickly onboarding a new Claude — not a full history. The **git log** is the full history. This file is the cliff notes.

---

## 11. Changelog (most recent first)

- **2026-07-06** — Combined release: five improvements. (1) Grace period on the Resume Working Here modal extended from 4s to 8s — accommodates slower sync init on cold starts. (2) **Sync egress reduction:** workspace switches no longer trigger a full sync push of every workspace + every tab. The SWITCH_WORKSPACE / CREATE_WORKSPACE / MOVE_TABS cases in pushToSync were pushing every workspace's every tab, dwarfing the actual delta. Now switches push just workspace rows (isActive flags) plus tabs only for affected workspaces; the incoming workspace's tabs get pushed by finalReconcile's normal snapshot after the switch settles. Sam's Supabase egress should drop dramatically. (3) **Device name kept fresh:** heartbeat now updates device_name from the current browser+OS. Previously the name was set once at first claim and never updated, so Sam's Firefox row showed as "Chrome on Windows". (4) **PIP tracker code removed.** Since 0.1.46's persistent-tab feature replaced auto-PIP-detection, the pip-tracker content script and pipActiveTabs machinery were dead code. Removed the content script (one less script running on every page), the PIP_STATE_CHANGED / QUERY_PIP_STATE message plumbing, the onCheckPipActive callback, and the pipActiveTabs Set + persistence helpers. (5) **Force Push/Pull buttons in popup:** two new buttons in the popup UI. "Force Push to Cloud" writes every local workspace + tab to Supabase, "Force Pull from Cloud" destructively pulls Supabase state into local. Both have confirmation prompts. Nuclear options for when sync gets weird. Shipped as 0.1.52.

- **2026-07-06** — Two follow-up fixes from Sam's 0.1.50 diagnostic. **Fix 1: Resume Working Here modal grace period.** The modal was showing on every SW restart because the newtab UI's `isActiveDevice` initializes to `'unknown'` and the modal rendered for that state. Sync init eventually confirms `'active'` but by then the user had often clicked Resume, running the claim flow unnecessarily. Sam's 0.1.50 trace showed SW start at 20:16:11, sync confirmed active at 20:16:14, user clicked Resume at 20:16:17 — the modal was showing for 3+ seconds while the SW was already confirming active status. Fix: added a 4-second grace period after mount during which the modal doesn't render while `isActiveDevice` is still `'unknown'`. New state `mountedAt = useState(() => Date.now())` and `graceExpired = useState(false)`; a new `useEffect` sets `graceExpired = true` 4s after mount if still `'unknown'`. Modal render condition changed from `isActiveDevice !== 'active'` to `isActiveDevice === 'inactive' || (isActiveDevice === 'unknown' && graceExpired)`. If sync confirms `'active'` within the window, the modal never appears. If we're actually inactive OR the grace period expires without confirmation, the modal shows normally. The existing `isActiveDevice === 'active' → setClaimError(null)` effect stays; combined with the new condition, transitioning to `'active'` reliably hides the modal. **Fix 2: Broader sweep of stale placeholder IDs.** 0.1.50's `sweepStalePlaceholderIds` sweep only ran on the active workspace (called from `reconcileToActiveWorkspace` before materialization). Sam's diagnostic showed `moved-17` still appearing in five inactive workspaces (Networks, Archery, Health&Fitness ×4, SPEAR). Added a broader sweep from the IIFE right AFTER `runSystemOperation('startupReconcile', ...)` completes: iterates ALL workspaces via `workspaceEngine.getWorkspaces(LOCAL_USER_ID)` and calls `tabManager.sweepStalePlaceholderIds(ws.id, storage)` on each. Per-workspace try/catch so one failure doesn't kill the pass. Total `renamed` + `deduped` counts logged via `logDiagnostic('cleanup', 'startup sweep across all workspaces', {...})` when non-zero. Ordering matters: the active workspace is already reconciled by the time this runs, so its records were swept during reconcile; the broader pass only touches inactive workspaces. Shipped as version **0.1.51**.

- **2026-07-06** — Four fixes from Sam's 0.1.49 diagnostic session. **Fix 1: Loud schema-mismatch detection.** `SupabaseSyncClient` now detects the "Could not find the 'X' column" PostgREST error in every push method's catch block, extracts the column name, sets a `tabflow_schema_missing_column` flag in `chrome.storage.local` via a new `onSchemaMissingColumn` constructor callback wired from `service-worker.ts`. `NewTab.tsx` polls the flag every 5s and shows a prominent yellow banner at the top of the TabFlow page with the exact `ALTER TABLE ... ADD COLUMN ... NOTIFY pgrst, 'reload schema';` SQL. When the column is `persistent`, we generate the exact SQL; otherwise a generic template. Banner has Copy SQL and Dismiss buttons; session-level dismiss auto-clears when a fresh occurrence is detected. Flag is cleared when a subsequent push of the same operation succeeds. No more silent sync failures — if Sam ever has to run migration SQL again, the extension will tell him exactly what to run and where. **Fix 2: Single-active-workspace invariant.** `IndexedDBAdapter.saveWorkspace` now runs inside a Dexie `rw` transaction when the incoming workspace has `isActive=true`: reads all workspaces for the user, clears `isActive` on every sibling, then puts the target. Atomic — Dexie rolls back the whole thing on any throw. `reconcileToActiveWorkspace` now also detects multiple `isActive=true` workspaces on startup, picks the one with the most recent `updatedAt`, clears the others (each write goes through the same atomic guard), and pushes the repair to Supabase. Sam's 0.1.49 diagnostic showed two workspaces both flagged active — this closes the invariant that let that state exist. **Fix 3: Diagnostic report URL matching unwraps `suspended.html`.** The "Active Workspace Detail" section of the Diagnose report canonicalized browser tab URLs directly, but the browser tabs are wrapped in `suspended.html?url=<real_url>` and never matched the DB records (which hold real URLs). Reported false "0 matching, 13 missing, 13 extra" divergences for what was actually a fully-matched state. Fix: before canonicalizing, unwrap URLs whose prefix matches `<extensionBaseUrl>suspended.html` via a small inline helper (same logic as `TabManager.getRealUrl`). **Fix 4: Stale `moved-*` / `dup-*` tab IDs cleaned up.** Sam's diagnostic showed `moved-17` appearing in multiple workspaces from unfinished move operations that outlived their gate. New `TabManager.sweepStalePlaceholderIds(workspaceId, storage)` iterates the workspace's DB records: for each `moved-<n>` or `dup-<n>`, if a proper `tab-<16hex>` record already exists for the same canonical URL, dedupe (delete the placeholder); otherwise regenerate via `computeTabId(workspaceId, url, createdAt)` and delete the old row. `reconcileToActiveWorkspace` calls this on startup for the active workspace, BEFORE materialization, so `restoreWorkspaceTabs` sees the cleaned state. `saveCurrentTabsToWorkspace`'s cleanup sweep no longer preserves placeholder IDs at all — they're deleted along with other orphans (a subsequent snapshot with the URL live re-mints a proper deterministic ID via the existing rewrite path). Every rename / dedupe / delete emits a `cleanup` diagnostic-log entry so the trail shows up in the Diagnose report. Shipped as version **0.1.50**.

- **2026-07-06** — Comprehensive sync-flow instrumentation for diagnosing Resume Working Here bugs. `claimActiveDeviceWithMaterialization` now logs every step (starting, syncClient ready, pullAll start/complete, materialize, claim). `SupabaseSyncClient` gained an `onDiagnostic` constructor callback; `pushWorkspace`, `pushTab`, `pushHistoryEntry`, `pushDeletedWorkspace`, `pullAll`, `claimActiveDevice`, and `isActiveDeviceFromDb` now log through it. Active-device state changes are logged with previous vs new state so races are visible. On SW startup, after sync init, a cloud-vs-local active-workspace comparison is logged so a Resume that pulls the wrong workspace can be traced back to whichever side had stale data. The Diagnose report gained "Recent Claim Events" and "Recent Sync Trace" sections that surface the claim + sync events specifically. Also fixed: the double-reconcile on browser restart. `chrome.runtime.onStartup` now checks whether the IIFE already ran the reconciler in the last 15 seconds and skips if so. Prevents the wasteful and race-prone back-to-back materialize sequence Sam captured in his 0.1.48 diagnostic. Shipped as version **0.1.49**.

- **2026-07-05** — User-controlled tab preservation across workspace switches. New checkbox on each tab card lets you mark a tab as "keep alive across switches." When set, that tab is hidden (via Firefox's `chrome.tabs.hide()` API) on workspace switch instead of closed, then unhidden on switch back to its workspace. Preserves Picture-in-Picture videos, form state, live audio, and anything else that would die on a normal close-and-recreate. Chosen because Firefox's PIP is invisible to WebExtensions APIs — we couldn't reliably auto-detect PIP, so this puts control in the user's hands.

  **Implementation:** New `persistent: boolean` field on the Tab schema (default false). Synced through Supabase (**existing users MUST re-run `tabflow-setup.sql` to add the column — without it, the new field silently fails to sync**). The `hiddenTabs` map in `chrome.storage.session` tracks storageId → chromeTabId for tabs currently hidden this session. On workspace switch away from a workspace with persistent tabs, those tabs get `chrome.tabs.hide()` and added to the map. On switch back, incoming DB tab records check the map first — if a valid hidden tab exists for that storageId, we `chrome.tabs.show()` it instead of creating fresh (verified alive via `chrome.tabs.get` first — stale entries fall back to normal creation).

  Session storage means the map resets on browser restart, matching PIP's own lifecycle. `chrome.tabs.onRemoved` prunes the map when a hidden tab is closed unexpectedly. A safety net on `chrome.tabs.onUpdated` re-hides tabs that Firefox may force-show (e.g., if dismissing PIP causes the source tab to reappear).

  Firefox-only feature — `chrome.tabs.hide()` doesn't exist in Chrome. Chrome builds show a disabled checkbox with a tooltip explaining the limitation. Requires the `tabHide` Firefox permission (added to `manifest.firefox.json`).

  Removed: the 0.1.38 PIP-aware `moveTabsToHiddenWindow` path is no longer called. The user's explicit `persistent` flag is the sole signal for tab preservation. The `pip-tracker` content script and `pipActiveTabs` tracking code stays for now (harmless, may be removed in a future cleanup pass) since Firefox's PIP is genuinely invisible to WebExtensions APIs anyway.

  Shipped as version **0.1.46**.

- **2026-07-02** — Added a visible "Diagnose" button on the TabFlow newtab page that gathers EVERY piece of state needed to diagnose a bug and copies a formatted plaintext report to clipboard in one click. Report includes: extension version, browser, extension state (tabFlowTabId + validity, hiddenWindows map + validity per window, pipActiveTabs, gate state, cachedMainWindowId, sync init state, isStartingUp/isSwitchingWorkspaces flags), all browser windows and their properties, tabs in the main window with URL/pinned/active/audible attributes, workspace list from DB, active workspace cross-checked against browser tabs (missing/extra), sync state (local device ID, active device per Supabase, heartbeat age), and the last 500 diagnostic log entries from 0.1.44. New `GET_DIAGNOSTIC_REPORT` message handler in the service worker builds the report via a `buildDiagnosticReport()` helper. The 0.1.44 Ctrl+Shift+D Debug panel stays as an advanced real-time view. Shipped as version **0.1.45**.

- **2026-07-01** — In-app diagnostic log viewer shipped as **0.1.44**. Task: existing SW `console.log` output is only accessible through the browser DevTools service-worker console, which is a pain to explain to non-technical users when debugging weird startup or PIP issues remotely. Fix: a new module `packages/browser-extension/src/background/DiagnosticLog.ts` exports `logDiagnostic(category, message, data?)`, `getDiagnosticLog()`, and `clearDiagnosticLog()`. Buffer is 500 entries, FIFO-evicted, persisted in `chrome.storage.local` under `tabflow_diagnostic_log`. Writes are debounced 200ms to avoid thrashing storage on rapid events (PIP state changes, etc.). Data payloads are JSON-stringified and truncated at 500 chars to prevent runaway storage growth. Each `logDiagnostic` call ALSO mirrors to `console.log` with the `[TabFlow:<category>]` prefix, so DevTools users see the same events. Categories: `startup`, `reconcile`, `pip`, `workspace-switch`, `error`. Instrumentation calls added at the critical points: IIFE start / gate preloaded / tabs settled; `reconcileToActiveWorkspace` at every step boundary; `PIP_STATE_CHANGED` handler and `onCheckPipActive` fast/slow-path results; `handleSwitchWorkspace` at start / PIP-check-result / outgoing-tabs-handled / complete; every caught error records a `{ step, error: String(err) }` payload. Two new message types added — `GET_DIAGNOSTIC_LOG` (returns entries) and `CLEAR_DIAGNOSTIC_LOG`. New MessageHandler constructor field `onDiagnostic` (optional callback, wired from `service-worker.ts`) so MessageHandler can log without a circular import back to the SW-local module. UI side: `NewTab.tsx` gained a `debugPanelOpen` state and a `Ctrl+Shift+D` keyboard shortcut that toggles it. When open, the panel polls `GET_DIAGNOSTIC_LOG` every 2 seconds and displays entries in a monospaced textarea (readonly, ISO timestamps, category tags, message + truncated data). Three action buttons: Copy to Clipboard, Clear Log, Close. Styles in `styles.ts` — `debugPanelBackdrop` uses `zIndex: 9990`, deliberately BELOW the operation overlay (10000) and Resume modal (9999) so those still block interaction if they're up. No changes to any operational code paths — this is pure instrumentation. The database-locked-down and Resume-Working-Here-testing rules from §3.1 and §3.4 don't apply here because no DB write and no sync-touching code was modified. Existing `console.log` calls are all preserved verbatim so DevTools debugging still works.

- **2026-06-22** — Two real bugs fixed (proper code-path investigation this time, not theory).

  **Bug 1: PIP dying instantly on workspace switch.** Root cause: the pip-tracker content script's QUERY_PIP_STATE handler (added in 0.1.40 for the live-query fallback) called `sendResponse()` synchronously THEN `return true`. Per Chrome's API contract, `return true` signals "I'll respond asynchronously, keep the channel open" — but the response was already sent synchronously. This left the response in an undefined state where the SW's awaited `chrome.tabs.sendMessage` may not receive it. The live PIP query then fell back to "no PIP detected", causing the workspace switch to close the outgoing tabs (including the PIP source tab) instead of moving them to a hidden window. PIP died at the moment of switch. Fix: remove the `return true` line. Synchronous responses don't need it.

  **Bug 2: Resume Working Here modal not showing on browser startup.** Root cause: after the startup reconciler closed non-TabFlow tabs and materialized workspace tabs, `backfillThumbnails` activated each workspace tab in sequence to capture screenshots. The finally block tried to restore focus to the "originally active" tab, but the sequence sometimes left a workspace tab active at the end. When the TabFlow newtab page wasn't the active tab, the browser throttled its JavaScript execution — React didn't mount, the device-status polling didn't fire, the Resume modal never rendered. User had to click "+" to manually open the TabFlow page. Fix: at the very end of `reconcileToActiveWorkspace` (after backfill), explicitly activate the TabFlow tab via `chrome.tabs.update(tabFlowTabId, { active: true })`. This guarantees TabFlow is visible immediately on browser startup, so React mounts, polling fires, and the Resume modal (or normal workspace view) appears without user intervention.

  Shipped as version **0.1.42**.

- **2026-06-22** — Fixed PIP being killed on every service worker wake. 0.1.38 had added a "close all non-main windows" step to the startup reconciler to clean up orphans. But MV3 service workers terminate when idle and restart whenever an event fires, and the IIFE runs the reconciler every time. So the sequence was: user pops PIP, switches workspace -> outgoing tabs move to hidden window (PIP preserved) -> SW idles and terminates -> ANY event wakes the SW -> IIFE runs reconciler -> reconciler closes the hidden window -> PIP dies. The 0.1.40 fix (persisting `pipActiveTabs`) was orthogonal to this and didn't help.

  Fix: `reconcileToActiveWorkspace` now reads the `hiddenWindows` map from chrome.storage.local before closing windows. Windows whose IDs are in the map are intentional (workspace tabs preserved for PIP) — skip them. Only orphan windows (not main, not in map) get closed. After the close pass, prune the map to remove entries whose window IDs no longer exist (catches stale entries from before this fix).

  On real browser restart: Firefox reassigns window IDs, so any session-restored windows have new IDs not in the map. They get closed correctly. PIP is lost across browser restart (acceptable — user already confirmed they don't care). Across SW wake within a session: hidden windows preserved, PIP keeps playing.

  Shipped as version **0.1.41**.

- **2026-06-22** — Fixed PIP being lost on workspace switch. 0.1.38 introduced PIP-aware hidden windows: workspace switch only creates a hidden window if an outgoing tab has active Picture-in-Picture. The check used an in-memory `Set<number>` (`pipActiveTabs`) populated by the `pip-tracker` content script's `PIP_STATE_CHANGED` messages. But MV3 service workers sleep when idle, wiping the in-memory Set. When the user switched workspaces after the SW slept, the empty Set made the switch think "no PIP" and the outgoing tabs were closed — killing the PIP video.

  **Three coordinated changes:**

  1. **Persist `pipActiveTabs` to `chrome.storage.session`.** New `persistPipActiveTabs()` writes the Set to session storage after every change. `restorePipActiveTabs()` reads it back on SW startup (called in the IIFE alongside `preloadGate()`). Session storage survives SW sleep/wake cycles but not browser restart — which matches PIP's lifecycle (PIP dies on browser close anyway).

  2. **Live query as a safety net.** The `onCheckPipActive` callback now checks the persisted Set first, then live-queries each outgoing tab's content script via `chrome.tabs.sendMessage({ type: 'QUERY_PIP_STATE' })`. The content script responds with `document.pictureInPictureElement` truthiness. Catches the rare case where a PIP_STATE_CHANGED message was lost or the SW restarted after PIP started.

  3. **pip-tracker content script handles QUERY_PIP_STATE.** Added a `chrome.runtime.onMessage` listener that responds synchronously with the current `document.pictureInPictureElement` state.

  Shipped as version **0.1.40**.

- **2026-06-22** — Fixed "Sync not initialized" error race in Resume Working Here. When the user clicked Resume shortly after Firefox started, the `CLAIM_ACTIVE_DEVICE` handler could return that error because the SW's `syncClient` hadn't finished initializing yet (the IIFE sets it up asynchronously after gate preload, tab settle, and reconcile). The UI showed a persistent red error even though the actual claim usually succeeded on a retry. Fix: three coordinated changes. (1) `CLAIM_ACTIVE_DEVICE`, `RESTORE_FROM_CLOUD`, and `claimActiveDeviceWithMaterialization` now wait up to 5 seconds (polling every 100ms) for `syncClient` and `syncUserId` to be ready before returning an error. Real failures (e.g., user not signed in) still surface the error after the timeout. A new helper `waitForSyncReady(timeoutMs=5000, pollMs=100)` lives in `service-worker.ts` next to the `syncClient` / `syncUserId` declarations and is awaited at the top of each handler. (2) `NewTab.tsx` adds a `useEffect` that clears `claimError` when `isActiveDevice` transitions to `'active'`, so any stale error from a previous attempt disappears when the claim eventually succeeds — covers both the user's own retry-click path and the case where another tab / popup in the same browser claimed asynchronously. (3) The error display is now gated by `claimError && !claimInProgress && isActiveDevice !== 'active'`, preventing the "I see an error but tabs materialized" confusion. Shipped as version **0.1.39**.

- **2026-06-22** — PIP-aware hidden windows + browser-close cleanup. Fixes the "Firefox stays alive as a background process" bug. Before this fix: every workspace switch moved the outgoing workspace's tabs into a minimized Firefox window with WS_EX_TOOLWINDOW so it wouldn't show in the taskbar. These windows persisted across the user clicking X to close Firefox — the visible window closed but Firefox's process kept running (8+ GB memory, 39 sub-processes) because the hidden windows looked like "open windows" to Firefox. On "reopen," Firefox just opened a new window in the existing process; no startup events fired; the reconciler never ran; the user saw a stray Puget Systems homepage tab. **Three coordinated changes:** (1) **New `pip-tracker` content script** runs on all pages at `document_idle`, listens for HTML5 `enterpictureinpicture` / `leavepictureinpicture` events on video elements (including dynamically-added ones via MutationObserver), and sends `PIP_STATE_CHANGED` to the background. The background maintains `pipActiveTabs: Set<number>` and prunes entries on `chrome.tabs.onRemoved` so stale tab IDs can't leak. Skips TabFlow's own newtab page. Re-checks PIP state on visibilitychange so Firefox's lazy-restored PIP doesn't slip past. (2) **PIP-aware workspace switch:** `MessageHandler.handleSwitchWorkspace` got a new `onCheckPipActive(tabIds)` callback wired from the SW. Before deciding what to do with the outgoing workspace's tabs, it enumerates them and calls the callback. If true, `moveTabsToHiddenWindow` runs as before. If false (the common case), `closeAllTabs` runs and no hidden window is created. (3) **Browser-close cleanup:** new `chrome.windows.onRemoved` listener in `service-worker.ts` reads the `hiddenWindows` map from `chrome.storage.local`, enumerates remaining windows, and — if there are zero visible non-minimized non-hidden windows left — closes every hidden window so the browser process can exit cleanly. The closed window's entry is pruned from the map first; if other visible windows remain, only the closed entry is removed. Wrapped in try/catch at every step so the listener can never throw and crash the SW. **Startup reconciler updated** to close ALL non-main windows (not just non-TabFlow tabs in the main window). This handles any leftover hidden windows from 0.1.37 installs upgrading to 0.1.38, plus stray Firefox-restored homepage windows, plus any other orphans. The `hiddenWindows` map is reset to `{}` after the close pass. CLAUDE.md §5.9 architecture note rewritten to reflect the new policy. Trade-off accepted: PIP no longer survives browser restart (user confirmed they don't care). PIP across workspace switch within a session still works as before — the hidden-window mechanism is unchanged; we just don't create the hidden window unless PIP is actually active. New content script registered in both manifests as a third `<all_urls>` entry alongside `scroll-tracker` and `operation-overlay`. Both manifests bumped to 0.1.38. No new permissions required. Shipped as version **0.1.38**.

- **2026-06-22** — Cross-device sync gap closure: workspace history, deleted-workspaces archive, workspace shortName, and sidebar-width-on-Resume now sync via Supabase. Per the VISION_AUDIT findings, these four features were local-only — every device kept its own copy and they never followed the user across machines. Fix landed in one coordinated pass shipped as **0.1.37**. **Schema changes (existing users MUST re-run `tabflow-setup.sql` on their Supabase project — without that, the new pushes fail with 42P01 "relation does not exist" and history/archive/shortName silently stop syncing):** new `workspace_history` table (encrypted JSONB tab snapshots, indexed by user+workspace+created_at DESC, RLS and GRANTs included, 30-day client-side retention via `pruneOldHistory`); new `deleted_workspaces` table (encrypted JSONB of `{ workspace, tabs }` at deletion time, indexed by user+deleted_at DESC, 90-day retention); `workspaces` table gained `short_name TEXT` column (plaintext, nullable, inherits parent grants). Mirrored into both the standalone `packages/supabase/tabflow-setup.sql` (with full comments) and the embedded `SETUP_SQL` in `SetupWizard.tsx`. New migration file `packages/supabase/migrations/006_history_archive_sync.sql` (number 005 was already taken by 005_tab_scroll_position.sql, so the next available is 006). **Sync client:** `SupabaseSyncClient` gained `pushHistoryEntry(entry, userId)`, `pullHistory(userId)`, `pruneOldHistory(userId)`, `pushDeletedWorkspace(entry, userId)`, `pushRestoredWorkspace(deletedId, userId)`, `pullDeletedWorkspaces(userId)`, and `pruneOldDeletedWorkspaces(userId)`. All payloads encrypted with the same passphrase-derived AES-GCM-256 key as tabs/workspaces (JSON.stringify then `encrypt()`, base64-stored in JSONB columns). Decryption failures on pull are logged and skipped per row — they don't break the whole pull. `pushWorkspace` now includes `short_name: workspace.shortName ?? null` in the upsert. `pullAll` reads `short_name` directly from the cloud row instead of preserving from a local `shortNameMap` — the old preservation hack is removed. **Service worker:** the snapshot history-save block now also calls `pushHistoryEntry` (gated on active device per DB + safeToPush, same as snapshot tab pushes) with a fire-and-forget `pruneOldHistory` chaser. `MessageHandler` gained two new optional callbacks (`onPushDeletedWorkspaceToCloud`, `onPushRestoredWorkspaceToCloud`) wired from the service worker so `handleDeleteWorkspace` pushes the archive entry to cloud after the local archive, and both `handleRestoreDeletedWorkspaces` and `handlePermanentlyDeleteWorkspaces` delete from cloud after the local removal. The callbacks no-op cleanly when not active / not safe to push — local writes are never blocked by cloud failures. `claimActiveDeviceWithMaterialization` now ALSO pulls history (`clearWorkspaceHistory` then `pullHistory` + `saveHistoryEntry` loop), archive (`clearDeletedWorkspaces` then `pullDeletedWorkspaces` + `archiveWorkspace` loop), and preferences (mirrors `sidebarWidth` to `chrome.storage.local`) after the workspaces/tabs `pullAll(destructive=true)` returns. Each pull is wrapped in its own try/catch — an individual pull failure doesn't break the claim, since workspaces+tabs already succeeded. **IndexedDBAdapter** gained `clearWorkspaceHistory()`, `clearDeletedWorkspaces()`, and `getDeletedWorkspaceById()` helpers (the first two used by the claim flow's destructive pull; the third available for future callers). **Tests/verification:** every modified file passes `esbuild --bundle=false` parse; release zips contain `pushHistoryEntry`, `pullHistory`, `pushDeletedWorkspace`, `pullDeletedWorkspaces`, `short_name`, `workspace_history`, and `deleted_workspaces`. The 0.1.36 fixes (waitForTabsToSettle, history-restore tab filter, pin verification) are untouched.

- **2026-06-12** — Fixed three Firefox startup bugs that were causing the install-data-loss scenario the user reported on 0.1.35. **Bug 1: reconciler ran before Firefox finished session restore.** The IIFE waited a fixed 200ms then ran `reconcileToActiveWorkspace`. Firefox's session restore typically takes 500-800ms, so the reconciler operated against an incomplete tab state - session-restored stray tabs (like a Puget Systems homepage the user wasn't expecting) appeared AFTER the reconciler completed and were never closed. Fix: replaced the fixed 200ms with `waitForTabsToSettle()` - the IIFE now tracks the timestamp of the last `chrome.tabs.onCreated` event and waits until 1.5 seconds have passed with no new tab creations (minimum wait 500ms, maximum wait 8s). The tracker listener is registered at module top-level (before any awaits) so events during preloadGate/migrateTabIdsV1 still count. After session restore completes, no more onCreated events fire, the settle threshold is reached, and the reconciler runs against the final state. **Bug 2: history restore was closing the TabFlow tab when it was unpinned.** `handleRestoreHistoryEntry`'s filter was `!ct.pinned && ct.url !== 'chrome://newtab/'`, which excluded `chrome://newtab/` but not the actual TabFlow newtab URL (`chrome-extension://<id>/newtab.html` or the Firefox `moz-extension://` equivalent). If startup left the TabFlow tab unpinned (which the user observed), history restore closed it along with everything else, leaving the user with no remaining tabs - felt like the browser shutting down. Fix: filter now uses the stored `tabFlowTabId` AND a URL match against `getExtensionPageUrl('newtab.html')` to protect the TabFlow tab regardless of pinned state. Both `getExtensionBaseUrl` and `getExtensionPageUrl` are imported from `browser-compat.ts`. **Bug 3: `ensureTabFlowTab` didn't pin consistently across all code paths.** The "stored ID matched" return path didn't always pin if the existing tab had been unpinned by Firefox session restore - the existing pin step only fired when `keeper.pinned` reported false at query time, which can lag the actual browser state. Fix: a unified pin-verification step at the bottom of `ensureTabFlowTab` (just before duplicate-close cleanup) calls `chrome.tabs.get(id)` to read the live state and `chrome.tabs.update(id, { pinned: true })` if not already pinned. Every return path now ends with this verification. Heavy console.log coverage across all three fixes so users can share SW output if startup still fails. Shipped as version **0.1.36**.

- **2026-06-11** — Fixed the confusing tab-flicker users saw during the post-workspace-switch thumbnail backfill. The 0.1.34 blocking modal lives on the TabFlow newtab page only — when backfill activated a workspace tab to capture its thumbnail, the user briefly saw that workspace tab (without the modal) while focus was elsewhere. Fix: new content script `operation-overlay.ts` injected on `<all_urls>` at `document_start` that polls the service worker's `GET_OPERATION_STATUS` every 250ms. When an operation is in progress, the script renders a full-screen overlay (same styling as the React modal — dark backdrop with blur, centered card with spinner, friendly title, "Please wait" subtitle) on whatever page is currently active. When the operation completes, the overlay is removed. The content script skips itself on TabFlow's own newtab page (which the React modal already covers). z-index `2147483647` ensures the overlay sits above page content. The poll is cheap (~30 messages/sec across a typical workspace), and exceptions during polling (e.g., service worker reloading) safely hide the overlay. Net effect: from the user's perspective, the operation modal is always visible regardless of which tab the browser has activated for backfill — no more confusing flicker mid-switch. Shipped as version **0.1.35**.

- **2026-06-11** — Fixed the post-workspace-switch tab activation race by merging thumbnail backfill into the workspace switch operation, and replaced the small top-banner loading overlay with a centered blocking modal. The previous flow scheduled `backfillThumbnails` via `setTimeout(..., 1000)` AFTER `handleSwitchWorkspace` completed and the gate cleared. If the user clicked a tab card during that 1-second window, their click would race against the backfill's tab activations — they'd see the active tab jump to other workspace tabs before settling. Fix: removed the fire-and-forget scheduler entirely; backfill now runs inline at the end of the switch operation, BEFORE `runSystemOperation` returns, so the gate stays set and tab activations from backfill cannot interleave with user clicks. Same treatment applied to `reconcileToActiveWorkspace` (startup) and `claimActiveDeviceWithMaterialization` (Resume Working Here) — each materializes its workspace's tabs, then immediately runs backfill, then returns. The UI overlay was simultaneously upgraded from a thin top banner (`pointer-events: none`, easy to miss) to a centered full-screen blocking modal styled like the Resume Working Here modal — large spinner, friendly title ("Switching workspace…"), explanatory subtitle, dark backdrop with blur. The modal explicitly blocks clicks because the operation is in progress. YouTube video playback in workspace tabs is unaffected (the modal lives on the TabFlow newtab page; videos play in other tabs). `SystemOperationGate`'s `MAX_GATE_LIFETIME_MS` was bumped from 60s to 5 minutes so the gate doesn't auto-clear during a long backfill (the modal is blocking, so a long wait is acceptable). Per-tab backfill time tightened from ~500ms (250+250) to ~400ms (200+200). The MessageHandler callback was renamed from `onScheduleBackfillThumbnails: (id) => void` to `onPostOperationBackfill: (id) => Promise<void>` so the operation awaits backfill before clearing the gate. `backfillThumbnails`'s entry guard on `isSwitchingWorkspaces || isStartingUp || isGateSetSync()` was removed — those are always true during inline use; cancellation is now strictly via the per-call `currentBackfillToken` and user-activated-a-different-browser-tab detection. The `isBackfillingThumbnails` flag still exists harmlessly as belt-and-suspenders for tab event listeners. Net effect: the user sees a clear "wait" indicator during operations and cannot interact with stale UI. After the modal closes, all the workspace's thumbnails are already captured. Shipped as version **0.1.34**.

- **2026-06-10** — DB-as-truth startup refactor. Replaced the destructive `runChromeRestartFlow` (and all its detection logic — session markers, version comparison, startup-completed flag, Step 3 safety check) with a single non-destructive `reconcileToActiveWorkspace` function. Algorithm: find/create TabFlow tab via the 0.1.32 URL search, un-minimize the main window if needed, close every non-TabFlow tab in the main window, materialize the active workspace's tabs from DB via `restoreWorkspaceTabs`, then `saveCurrentTabsToWorkspace` to sync DB back. Wrapped in `runSystemOperation('startupReconcile', ...)` so tab events fired during it are suppressed. Called from both the IIFE (on every SW startup) and `chrome.runtime.onStartup` (on browser restart) — idempotent, the second call is a no-op since the browser already matches DB. Hidden workspace windows for INACTIVE workspaces are deliberately left alone — preserves the PIP-survives-restart feature because hidden windows hold inactive workspace tabs with their runtime state intact. This choice is documented in §5.9 of CLAUDE.md so we remember it. Net effect: install-data-loss bugs from `runChromeRestartFlow` Step 3 closing the user's main window can no longer happen (the destructive code is gone). The browser always matches the DB after SW startup, regardless of what kind of startup it was. Combined with the existing principle that only user actions write to the DB (snapshot system catches new tabs, URL changes, removes, etc.), TabFlow is now genuinely self-correcting: any state drift gets fixed on the next SW startup. Also deleted: `runChromeRestartFlow` (~210 lines) and its window-created listener; the alarm-based startup retry (`STARTUP_ALARM_NAME`) and `messageStartupPromise`; the session marker (`iifeRanThisSession`); the version-comparison branch reads (`wasJustUpdated`, `lastSeenVersion` reads — the write is kept harmlessly); the `tabflow_startup_completed` flag; the `pendingChromeRestart` flag; the `isChromeRestart`/`postRestartSnapshotSuppressed` module-level vars; the `setSkipOutgoingSave(true)` calls related to startup; the 30-second dedupe watchdog; the `renameStaleTabIds` function (no longer needed — `migrateTabIdsV1` is the deterministic-ID migration that ran once long ago). `ensureStartupComplete` now just waits for `isStartingUp` to clear (5s max). `service-worker.ts` shrank from 3,185 lines to 2,580 (-605 / -19%). Shipped as version **0.1.33**.

- **2026-06-09** — Fixed the Firefox install/restart "extra window" bug at the actual root, after 0.1.21–0.1.25 had been patching symptoms. Three coordinated changes shipped as 0.1.32. **Fix 1: `ensureTabFlowTab` searches by URL before creating.** Previously, when the stored `tabFlowTabId` was stale (which happens on Firefox extension auto-update because Firefox reassigns tab IDs), the function fell through to creating a new TabFlow tab via `chrome.tabs.create`. The new tab's window was picked as "first tab's window" from `chrome.tabs.query({})`, which on Firefox can be a different window than the previous main. Result: a new TabFlow tab in a new (unpinned) state in one window, and the original main window with all the workspace tabs hidden behind it. Now: if the stored ID is stale, `chrome.tabs.query({})` filters by the canonical TabFlow newtab URL across all windows. If found, that tab is pinned and adopted as the main TabFlow tab. Only if NO TabFlow tab exists anywhere does it create a new one — and the new one is placed in `chrome.windows.getLastFocused()` rather than first-tab's window. Detailed logging at each branch. **Fix 2: `runChromeRestartFlow` Step 3 verifies main window exists before closing others.** Step 3 closes all non-TabFlow windows, which is fine when the TabFlow window ID is correct. When it's wrong, the user's only good window gets closed. Now the step queries `chrome.windows.getAll()` first, verifies the cached `tabFlowWindowId` is in the list, and aborts the entire flow with an error log if not. **Fix 3: Persistent `tabflow_startup_completed` flag.** Prevents re-triggering startup detection during the same browser session. Set in `chrome.storage.local` after any successful startup path; cleared by `chrome.runtime.onStartup` (real browser restart). The IIFE and `ensureStartupComplete` both check it before running their restart-detection branches. Net effect: Firefox auto-update no longer creates a duplicate TabFlow tab in a separate window, no longer closes the user's main window if the cached ID is stale, and the startup flow runs at most once per actual browser session.

- **2026-06-09** — Thumbnail reliability overhaul. Two coordinated parts. **Part 1: capture pipeline hardened.** TabManager now caches `chromeTabId → storageId` for the active workspace as a byproduct of `saveCurrentTabsToWorkspace`'s position-matching; `captureTabThumbnail` looks up the storage ID directly via `tabManager.getStorageIdForTab(tabId)` instead of redoing position-matching at capture time (eliminates a class of silent failures when multiple tabs share a URL). The 1500ms debounce on activation-triggered captures was removed — captures fire immediately when the user clicks a tab. Page-load-complete captures keep a 300ms debounce to let layout settle. The `thumbnailCaptureInProgress` set is now keyed by Chrome tab ID instead of URL so same-URL tabs don't block each other. Capture errors that aren't permission/tab-gone get one retry after 500ms. The newtab page polls `GET_THUMBNAILS` every 1s for the first 30s after the active workspace changes, then settles to every 5s — new captures appear in the UI roughly five times faster than before. **Part 2: bulk thumbnail backfill.** One second after a workspace switch completes (gate clear, finalReconcile done), a background pass iterates the new workspace's tabs, briefly activating each one that lacks a thumbnail, capturing, and moving on. ~250ms per tab. Eligibility filter: must be loaded, not at a suspended.html URL, not at a privileged URL. The pass restores focus to the originally-active tab when done. New module-level flag `isBackfillingThumbnails` keeps the bulk activations from triggering snapshot writes or workspace switches; tab event listeners check it alongside `isSwitchingWorkspaces` and `isGateSetSync()`. Cancellation: the pass aborts if the user starts a workspace switch, opens a system operation, or activates a different tab manually. An incrementing token guards against double-running. Net effect: a freshly-opened workspace fills in its thumbnails in the background instead of forcing the user to click each tile. Shipped as version **0.1.31**.

- **2026-06-09** — Phase 3 of the gate-hardening refactor: a UI loading overlay during system operations. New `MessageType.GET_OPERATION_STATUS` returns the current `SystemOperationGate` state via a new read-only helper `getGateStateRaw()` exported from `packages/browser-extension/src/background/SystemOperationGate.ts` (mirrors `isGateSet`'s max-lifetime auto-clear logic so the UI never sees a stuck gate). `NewTab.tsx` polls every 300ms; when an operation is in progress, a small floating banner appears top-center (~80px from the top) with a CSS-only spinner and a friendly label — "Switching workspace…", "Restoring from history…", "Loading from cloud…", "Moving tabs…", "Restoring session…". Banner is `pointer-events: none` so it never intercepts clicks and never blocks media playback (YouTube videos keep playing during a switch). Workspace sidebar items get a new `disabled` prop wired to `currentOperation !== null`; while disabled they render at 0.45 opacity with `cursor: progress` and their `onClick` short-circuits to `undefined` so click-spam during a switch produces no queued operations. The `@keyframes tabflow-spin` rule is injected once on mount via a small `<style>` tag (cleaned up on unmount). New styles `operationOverlay` and `operationOverlaySpinner` added to `packages/browser-extension/src/newtab/styles.ts`. Shipped as version **0.1.30**.

- **2026-06-09** — Two follow-up bugfixes shipped together as version **0.1.29**.

  (1) **PIP pop-back auto-switch.** User reported: watching a video on Workspace A (e.g. YouTube), popping it into Picture-in-Picture, switching to Workspace B, then popping the PIP back into its source tab. Firefox un-minimizes the hidden source window to land the video back into a regular tab. The hidden window now has focus, but TabFlow still thinks Workspace B is active — the user sees Workspace A's tabs but no pinned TabFlow tab in the focused window. Fix: added a `chrome.windows.onFocusChanged` listener in `packages/browser-extension/src/background/service-worker.ts` that fires whenever any browser window gains focus. Bail conditions match the rest of the tab event listeners: `WINDOW_ID_NONE` (focus left the browser entirely), `isStartingUp`, `isSwitchingWorkspaces`, `isGateSetSync()`, and `windowId === cachedMainWindowId` all short-circuit. If focus landed on a window in the hidden-window map, look up the workspace, defensively confirm it's not already the active workspace, and dispatch `MessageType.SWITCH_WORKSPACE` through the existing `messageHandler.handleMessage()` route — same code path the UI uses when the user clicks a workspace in the sidebar. `handleSwitchWorkspace` already handles the "incoming workspace's tabs live in a hidden window" case via `restoreTabsFromHiddenWindow`, so no changes needed there: the outgoing workspace's tabs move from the main window to a new hidden window, the incoming workspace's tabs move from the now-focused (formerly hidden) window back to the main window, and the user ends up in the same window topology as any normal workspace switch. New `TabManager.getWorkspaceForHiddenWindow(windowId: number): Promise<string | null>` does the reverse lookup against the existing `hiddenWindows` Record in `chrome.storage.local`. Chrome's PIP doesn't typically un-minimize the source window the same way Firefox does, but the listener works for both — Chrome will just rarely have the listener trigger because its focus changes don't land on hidden windows during PIP pop-back.

  (2) **Version line restored in the new-tab page header.** The 2026-04-23 changelog claimed `TabFlow v0.1.7` appeared next to the memory stats in the header, but the line was gated by `systemMemory.total > 0` on the entire `memoryBlock` — meaning anyone without the native host installed (i.e. most users, since the host has to be installed separately via `install.bat` / `install-firefox.bat`) never saw it. The hardcoded "Chrome" label in the memory line also never updated to show "Firefox" on Firefox builds. Fix in `packages/browser-extension/src/newtab/NewTab.tsx`: added module-level constants `TABFLOW_VERSION = chrome.runtime.getManifest().version` and `BROWSER_LABEL = isFirefox ? 'Firefox' : 'Chrome'`. The `memoryBlock` is now always rendered; when the native host isn't available the System line and `<BROWSER>` line conditionally appear based on `systemMemory.total > 0` and `chromeMemory > 0` respectively, but the `TabFlow v<VERSION>` line always shows. Swapped the hardcoded `"Chrome"` literal for `{BROWSER_LABEL}` so Firefox builds correctly display "Firefox". Tree-shaking leaves only the correct literal in each bundle.

- **2026-06-09** — Phase 2 of the gate-hardening refactor (see REFACTOR_PLAN.md). Added a `finalReconcile(workspaceId, operationName)` helper called at the end of each system operation, before the SystemOperationGate clears. It settles 200ms then re-snapshots the active workspace to the DB. Catches two cases the prior pattern missed: (1) tabs the operation opened that hadn't fully registered when the main body returned, (2) any user actions during the operation (which the gate suppressed, but which still represent legitimate user intent). All 5 system operations now end with `finalReconcile`: `handleSwitchWorkspace` (reconciles destination workspace; replaces the explicit `saveCurrentTabsToWorkspace` near the end of the handler), `handleRestoreHistoryEntry` (reconciles restored workspace when active), `claimActiveDeviceWithMaterialization` (replaces the old re-snapshot), `handleMoveTabsInServiceWorker` (reconciles source workspace where tabs were closed; destination workspace's records are written directly and its tabs aren't in the live window), `runChromeRestartFlow` (reconciles active workspace after Step 6's tab restoration; the 30-second dedupe watchdog still runs separately after). Wired through to MessageHandler via a new optional `onFinalReconcile` constructor callback (different module from service-worker; can't import the helper directly without a cycle). Also removed the 0.1.21 "preserve records created within last 5 seconds" rule from `saveCurrentTabsToWorkspace` — it was a band-aid against snapshots firing mid-operation, which Phase 1's robust gate already prevents. Snapshots now run only when the gate is clear, by which point all created tabs are queryable and cleanup can trust chrome.tabs.query as truth. The "preservedPending" (in-flight moved-/dup- placeholders) and "preservedInert" (cross-browser privileged URLs) preservation rules stay — they protect against unrelated cases. Shipped as version **0.1.28**.

- **2026-05-13** — Phase 1 of the gate-hardening refactor (see REFACTOR_PLAN.md). Added a new `SystemOperationGate` module that persists "operation in progress" state across SW restart via chrome.storage.session. All five system operations (`handleSwitchWorkspace`, `handleRestoreHistoryEntry`, `claimActiveDeviceWithMaterialization`, `handleMoveTabsInServiceWorker`, `runChromeRestartFlow`) now use a `runSystemOperation(name, fn)` wrapper that handles set/clear in try/finally. Tab event listeners check the persisted gate via `isGateSetSync()` in addition to the existing in-memory `isSwitchingWorkspaces` flag. Critical bug from the audit: `handleMoveTabsInServiceWorker`'s nested setTimeout-based gate management was leak-prone (gate could stick ON if SW crashed between the inner timeouts); refactored to use proper async/await within the wrapper. Max-lifetime safety: gate auto-clears after 60 seconds even if something forgets to clear it (defensive). 0.1.21's "preserve recently-created records" rule stays in place as belt-and-suspenders. This phase addresses the gate's reliability; Phase 2 will add a final-reconcile step at the end of each operation. Shipped as version **0.1.27**.

- **2026-05-13** — Phase 0 of the gate-hardening refactor (see REFACTOR_PLAN.md). Reverted 0.1.24's verify-and-retry in restoreWorkspaceTabs. That block was tightening a symptom (incomplete restoration) by creating duplicates: when chrome.tabs.create resolved before the tab's URL settled, the verify check saw the tab as missing and created another one. Sam observed this as "all my AI workspace tabs duplicated on restore" and "moving a tab between workspaces creates two copies." Removing the retry stops the duplication. Underlying restoration unreliability remains (Phase 1 will fix the gate's leaks); restoration "sticking on first try" is a Phase 1 goal, not Phase 0.

- **2026-05-13** — Switched extension-update detection from session-marker to version-comparison after 0.1.24 still lost tabs on install. Hypothesis confirmed: `chrome.storage.session` on Firefox is cleared on extension auto-update (Firefox effectively uninstalls and reinstalls the extension), so the 0.1.23/0.1.24 session marker (`iifeRanThisSession`) was wiped before the new SW could check it. The IIFE / ensureStartupComplete fell through to stale-tabFlowTabId detection, set pendingChromeRestart, ran runChromeRestartFlow, and Step 3 closed all non-TabFlow windows — destroying Sam's hidden YouTube workspace window again. Fix in version **0.1.25**: added version-based detection alongside the session marker. The IIFE compares `chrome.storage.local.lastSeenVersion` (persisted across update — local storage IS preserved) against `chrome.runtime.getManifest().version`. If they differ, an extension update just happened. Either signal (session marker OR version mismatch) triggers the "skip Chrome-restart detection" branch. ensureStartupComplete has matching logic. This makes the detection robust against the Firefox session-storage quirk. Combined with all prior preservation fixes (0.1.21 record preservation, 0.1.22 closeAllHiddenWindows removal, 0.1.23 IIFE marker, 0.1.24 ensureStartupComplete marker + restore verify-retry), the install-causes-data-loss bug should finally be fully closed. If it still recurs, the SW console logs from the next install will show which branch fired and why — the new code logs the version comparison and marker check explicitly.

- **2026-05-13** — Fixed two layered issues that kept Sam losing tabs on every TabFlow install via Firefox auto-update. (1) **Session-marker check applied to `ensureStartupComplete`.** 0.1.23 added the marker check to the IIFE, but `ensureStartupComplete` (called at the top of every message handler) had its own stale-tabFlowTabId detection that also triggered `runChromeRestartFlow`. After install, the very next polling message (`GET_DEVICE_STATUS` every 3 seconds) would hit `ensureStartupComplete`, see the stale tab ID, set `pendingChromeRestart`, and trigger the destructive flow — destroying hidden workspace windows from the *other* code path I hadn't protected. 0.1.24 adds the same marker check there: if `iifeRanThisSession` is set, treat as extension reload regardless of tab ID staleness. (2) **Verify-and-retry in `restoreWorkspaceTabs`.** Even when hidden windows ARE preserved, the fallback restoration path has timing races: `chrome.tabs.create` can resolve without the resulting tab actually appearing in `chrome.tabs.query`, especially on extension auto-update mid-operation. 0.1.24 adds a post-creation verification loop — count expected URLs (with duplicate handling), measure live counts, retry creates for missing tabs up to 3 attempts with 500ms delays. Combined effect: install-induced data loss is closed off at the source (no spurious runChromeRestartFlow trigger), AND any remaining restoration unreliability is caught and fixed by the verify-retry. Sam's existing lost data still requires history restore to recover.

- **2026-05-13** — Fixed the actual root cause of install-causes-data-loss after 0.1.22 was insufficient. Sam reported continued data loss in his YouTube workspace after every install — 0.1.22 had removed `closeAllHiddenWindows()` from the IIFE but the hidden windows were STILL being destroyed. Investigation: the IIFE has a separate Chrome-restart detection path that fires `runChromeRestartFlow` (whose Step 3 closes all non-TabFlow windows). The detection heuristic: if `tabFlowTabId` doesn't resolve to a real tab, treat as Chrome restart. **Firefox extension auto-update reassigns numeric tab IDs even though the browser itself didn't restart** — so the IIFE saw the stored ID as stale, declared a Chrome restart, and ran the cleanup flow. Fix in version **0.1.23**: added a session marker via `chrome.storage.session`. The marker persists across SW reload (extension update, idle timeout, manual reload) but is cleared by the browser on actual restart. IIFE checks the marker first: if present, skip Chrome-restart detection entirely (we know this is an extension reload, not a browser restart). If absent, fall through to existing logic (genuine browser restart path). 0.1.22's `rehideInactiveWorkspaceWindows` + the closeAllHiddenWindows removal remain in place. Combined effect: hidden workspace windows are reliably preserved across extension auto-updates. Sam's YouTube tabs should no longer be destroyed on install. Sam's existing tab data is still lost from the previous incidents — history restore is the only way to recover, and with this fix plus the 0.1.21 record preservation, the restore should stick on the first try.

- **2026-05-13** — Fixed the root cause of the install-causes-data-loss bug instead of patching its downstream symptoms. Previous versions destroyed all inactive workspace tabs at the browser level on every SW reload (extension update, extension reload, anything that restarts the service worker). The IIFE's first act was `tabManager.closeAllHiddenWindows()` — intended to prevent stray hidden windows from being confusing in the taskbar after a reload, but the cost was destroying every tab the user had stashed in non-active workspaces. The storage records survived, so the system would later try to re-materialize them via `restoreWorkspaceTabs` on the next workspace switch, but that path has chrome.tabs.create timing races: the snapshot fires before all the tabs have registered with chrome.tabs.query, sees fewer live tabs than expected, and treats the missing ones as "closed-tab orphans" to delete. 0.1.21's 5-second record preservation prevented the records from being deleted (cards survived) but didn't actually open the browser tabs (only 6 of Sam's 14 YouTube tabs re-materialized after install). Fix in version **0.1.22**: removed `closeAllHiddenWindows()` from the IIFE. Hidden windows persist across SW reload — Firefox keeps all browser windows alive when the extension restarts, so the YouTube workspace's hidden window with all 14 tabs is still there after the update. Switching back to YouTube finds the hidden window via `restoreTabsFromHiddenWindow` and moves the tabs back to the main window with their full state intact (video playback, scroll, forms — everything). No re-creation, no race, no data loss. Trade-off: the WS_EX_TOOLWINDOW style on those minimized windows may briefly be reset by Firefox's window management, making them visible in the taskbar for a moment until the native host reconnects. Added `TabManager.rehideInactiveWorkspaceWindows()` called 500ms after native host init to re-apply the style. Mild UX annoyance vs. catastrophic data loss is an easy trade. `runChromeRestartFlow` (which runs only on full browser restart, not extension reload) still handles cleanup of stale hidden-window-map entries from session-restored windows, so that path isn't broken. The 0.1.21 record preservation stays as belt-and-suspenders for any path that still has to materialize from storage (e.g. history restore, a hidden window that genuinely got destroyed).

- **2026-05-13** — Fixed a timing bug in `saveCurrentTabsToWorkspace`'s cleanup sweep that was destroying freshly-restored tabs across multiple paths. Bug Sam reproduced consistently across versions: install a new TabFlow version while on a different workspace -> click Resume Working Here -> switch back to YouTube workspace -> only 3 of his 14 tabs remain, and restore-from-history "sticks" only after multiple attempts. Root cause: when `restoreWorkspaceTabs` opens 14 suspended tabs via 14 sequential `chrome.tabs.create` calls, the tabs don't all appear in `chrome.tabs.query` immediately — tab registration is asynchronous. The snapshot fires (after the 300ms switch settle or 500ms restore settle), sees maybe 3 live tabs, and the cleanup sweep deletes the other 11 storage records as "closed-tab orphans." The 0.1.18 strict cloud mirror then pushes the now-3-tab state to Supabase, deleting 11 from cloud too. Fresh records gone from both local and cloud. Fix in version **0.1.21**: `saveCurrentTabsToWorkspace`'s cleanup sweep now skips deletion of any record with `createdAt` (or fallback `updatedAt`) within the last 5 seconds. These are freshly-restored tabs whose browser counterparts are still materializing; they get a grace period to register. Records older than 5 seconds are still treated as legitimate close-tab orphans and cleaned up normally. This covers ALL restore paths uniformly — workspace switch, history restore, claim materialization, extension-reload recovery — without each path needing its own gating logic. Combined with the existing `isSwitchingWorkspaces` gate, restores should now stick on the first try regardless of how many tabs are being restored. The 5-second window is generous enough for slow systems opening many tabs, but short enough that legitimate quick-close actions (user closes a tab within 5s of creating it) still propagate correctly. Sam's existing 11-tab loss in YouTube workspace must still be recovered via history restore — the fix prevents future occurrences but doesn't undo past damage.

- **2026-05-13** — Switched thumbnail storage from URL-keyed to tab-ID-keyed in version **0.1.20**. Sam reported all his YouTube workspace cards showing the same thumbnail because he keeps multiple tabs at `youtube.com/` (legitimately — he navigates back to the homepage between videos). The thumbnail system stored screenshots keyed by URL: `thumbnails[tab.url]`. All same-URL tabs shared one entry, and clicking any one of them caused captureVisibleTab to overwrite the shared entry — flipping every card's thumbnail to the latest screenshot. Fix touches four spots: (1) `IndexedDBAdapter.ts` renamed `saveThumbnail(url, dataUrl)` and `getThumbnails(urls)` parameters to `tabId`/`tabIds` for clarity (the underlying IndexedDB key remains the same string column). (2) `service-worker.ts` thumbnail capture path now looks up the storage record corresponding to the captured chrome tab by canonical URL + position-among-same-URL — mirroring the matching logic in `saveCurrentTabsToWorkspace`. The thumbnail is stored under the storage record's deterministic ID. (3) `service-worker.ts` `GET_THUMBNAILS` handler now accepts `tabIds` payload (with backward-compat for `urls` from any stale call sites). (4) `NewTab.tsx` builds the request payload from `tab.id` instead of `tab.url`, and renders cards with `thumbnails[tab.id]` lookup. Side effect: existing URL-keyed thumbnails in Sam's IndexedDB are orphaned and won't be retrieved by the new lookup. Cards start with empty thumbnails after the upgrade and re-populate as he views tabs (`scheduleThumbnailCapture` fires on tab activation). The LRU eviction (max 500 thumbnails) will eventually clean the orphans.

- **2026-05-13** — Fixed two interacting bugs that caused tabs to be silently deleted on Sam's install after 0.1.18 shipped. (1) **Removed 0.1.17's `restoreWorkspaceTabs` dedup.** That dedup was added as a band-aid for cloud-accumulated duplicate rows from past bugs. But 0.1.18's strict cloud mirror already prevents accumulation by design, so the band-aid is unnecessary — and worse, it actively destroys legitimate user-created duplicates (same URL opened intentionally in multiple tabs). On Sam's install, when 0.1.18's SW restart triggered a flow that re-opened workspace tabs from storage, the 0.1.17 dedup stripped 6 "duplicate" URLs out of his 13-tab YouTube workspace. The subsequent snapshot saw 7 live tabs, deleted the other 6 records from storage, and 0.1.18's strict mirror pushed the reduced state to Supabase. Sam lost 6 tabs in both local and cloud. (2) **`MessageHandler.handleRestoreHistoryEntry` was missing the `isSwitchingWorkspaces` gate.** The 2026-04-23 changelog claimed this fix was applied but it was never actually shipped (same pattern as the missing-polling regression caught in 0.1.16). Without the gate, the `chrome.tabs.remove(...)` calls that close outgoing tabs each fire `onRemoved` → debounced `snapshotActiveWorkspace` → cleanup sweep → deletes the freshly-saved DB records for the tabs the user JUST asked to restore. Fix: wrapped the entire flow in `this.onSwitchingWorkspacesChange?.(true)` / `(false)` (in a `try/finally` so the flag always lifts even on error), with a 500ms settle after `restoreWorkspaceTabs` so late tab events land while the gate is still up. Net effect: history restore now sticks on the first try; Firefox restart no longer silently deletes workspace tabs. The tension between Sam's "DB is source of truth" principle and the URL-derived tab IDs (which require the cleanup sweep so navigation doesn't accumulate orphan records) is documented but not yet resolved with a bigger refactor.

- **2026-05-13** — Fixed cloud sync to be a strict mirror of the active device's local state, not an additive upsert. Bug Sam hit: after the polling fix in 0.1.16 made Resume Working Here actually trigger reliably, he noticed his workspaces on the second PC had a mix of correct tabs and tabs from a *different* active workspace — tabs the source PC didn't currently have. Root cause was in `service-worker.ts` `snapshotActiveWorkspace`'s cloud push: after a snapshot wrote the current state to local IndexedDB, it pushed the local tabs to Supabase via `pushTab` (upsert) and only deleted the IDs the snapshot's local cleanup itself orphaned (`deletedTabIds`). Cloud rows that existed in Supabase but NOT in the current local state — accumulated from past bugs, workspace-switch races, or earlier sync hiccups — were invisible to this cleanup and persisted forever. Only `fullSyncPush` (which only runs on claim) cleaned the whole DB. Net effect: cloud's tab table accumulated orphan rows, and every Resume Working Here on the second PC faithfully reproduced the dirty state. Fix in version **0.1.18**: after the parallelized `pushTab` upserts, the snapshot now queries Supabase for the active workspace's full set of tab IDs via a new `SupabaseSyncClient.getTabIdsForWorkspace(workspaceId, userId)` helper, and deletes any IDs that aren't in the local set. Cloud's view of the active workspace exactly equals local's view after every snapshot — no orphan accumulation, no carryover from past bugs. Per-workspace cleanup (not whole-DB) because the snapshot only knows the active workspace's intended state; other workspaces get cleaned when they become active. Cost: one extra Supabase query per snapshot, which is cheap. The deepest behavioral fix here is that the design now matches what users actually expect ("cloud mirrors my current device") rather than the optimistic upsert-only model. Sam's existing dirty cloud data self-cleans the first time the active device on 0.1.18 does any snapshot — typically within seconds of installing.

- **2026-05-13** — Fixed duplicate tabs appearing in workspaces after Resume Working Here. Root cause: Supabase had accumulated multiple tab rows pointing at the same canonical URL within a single workspace (legacy artifact of past bugs across 0.1.x versions). `pullAll` faithfully wrote each row to local IndexedDB, then `TabManager.restoreWorkspaceTabs` opened one browser tab per record — N records for the same URL → N duplicate browser tabs visible in the workspace. The DB-as-source-of-truth design worked exactly as documented; the DB just had bad data and nothing was deduping at materialization time. Fix in version **0.1.17**: `restoreWorkspaceTabs` now dedupes its input by canonical URL before opening tabs (a `Set<string>` of seen canonical URLs gates the open loop). The subsequent `saveCurrentTabsToWorkspace` snapshot sees N unique live tabs, matches against the 2N stored records, deletes the unmatched orphan duplicates from IndexedDB. `fullSyncPush` then deletes the same orphan IDs from Supabase. One Resume Working Here cycle on this version self-cleans both local and cloud data. Sam should see duplicates disappear after the first Resume on whichever PC installs 0.1.17 first; the other PC's next pull gets clean data automatically.
- **2026-05-13** — Found and fixed a real regression in version **0.1.16**: the new-tab page's device-status check was one-shot on mount, with no recurring polling. The 2026-04-20 DB-only refactor removed the `storage.session.deviceStatus` broadcast from the service worker, intending to replace it with newtab-side polling — and the CLAUDE.md changelog from that day even *claims* polling was added, but it never actually was. The newtab kept a now-dead `chrome.storage.onChanged` listener for `deviceStatus`, which never fires anymore. Net effect Sam experienced: leave both PCs running, leave Firefox open on each. Go from PC2 to PC1, click Resume on PC1. PC2's TabFlow page sits there for the whole weekend with stale state — nothing notifies it of PC1's claim because the broadcast is dead and there's no poll. Walk back to PC2 Monday → no Resume Working Here modal because state still says "active." Only fix: open new tab + refresh, which remounts React, triggers a fresh one-shot check that finally sees the truth. Fix in `packages/browser-extension/src/newtab/NewTab.tsx`: replaced the single sendMessage with a recurring `setTimeout` chain polling every 3 seconds, plus a `visibilitychange` + `window.focus` listener that triggers an immediate re-poll whenever the tab regains focus (catches "user sat down at this PC after being away"). Removed the dead `changes.deviceStatus` listener; kept the `passphraseMismatch` listener since the SW does still broadcast that. The 0.1.14 tri-state + 5-second timeout fix didn't help because both only run on mount — once mounted with stale "active" state, nothing was telling the UI to re-check. This is the actual fix for the symptom Sam reported across multiple versions.
- **2026-05-13** — Fixed a deeper variant of the hover-expand bouncing bug that 0.1.13 only partially addressed. Original problem: when a user hovered a 2-line-clamped workspace name like "3D Printing & Desktop Manufacturing", expand widened the sidebar and the name reflowed to one line — moving up by ~16px and leaving the cursor in empty space below the item. The cursor genuinely (not phantom) left the item, item-level mouseleave fired, the 200ms collapse timer ran out, sidebar collapsed, name re-wrapped under the cursor, expand re-fired, loop. Version **0.1.15** moves the collapse trigger from per-item to sidebar-level: `onWantsHoverCollapse` from individual items is now a no-op, and `NewTab.tsx`'s sidebar `<div>` gained its own `onMouseEnter` (cancels any pending collapse) and `onMouseLeave` (schedules collapse with a 100ms debounce for CSS-transition phantom events). The new mental model: once expand fires, the sidebar stays expanded until the cursor exits the sidebar entirely. Cursor moving between items, between item and gap, or anywhere within the sidebar bounds keeps it expanded. Bouncing impossible because the cursor would have to actually leave the sidebar for collapse to fire.
- **2026-05-13** — Hardened the "Resume Working Here" flow after Sam was locked out of his work PC by a stuck modal in 0.1.13. Four coordinated changes in version **0.1.14**: (1) **Popup escape hatch.** `packages/browser-extension/src/popup/Popup.tsx` now always shows a "Force Claim This Device" button — independent of the new-tab modal's state, independent of polling. Click it, the popup sends `CLAIM_ACTIVE_DEVICE` directly to the service worker, and feedback appears inline. This is the new "I'm stuck" escape valve regardless of what UI state the new-tab page is in. (2) **Tri-state device status.** `NewTab.tsx`'s `isActiveDevice` was a boolean defaulting to `true` — if the first `GET_DEVICE_STATUS` poll failed or returned a transient error, the state stayed at "active" forever and the modal silently never appeared, even as `enforceTabLock` (which queries the DB on every call) kept yanking the user back to TabFlow. Changed to `'unknown' | 'active' | 'inactive'` defaulting to `'unknown'`. Modal renders for both `'unknown'` and `'inactive'`. Only an explicit `'active'` response hides it. No more "silently assume active". (3) **5-second safety timeout.** If no successful poll has confirmed device state within 5 seconds of mount, force the modal to show by flipping state from `'unknown'` to `'inactive'`. Tracked via a new `pollHasReturned` boolean. Real responses still override. Better to ask the user to click Resume than to leave them stuck. (4) **CLAUDE.md §3.4** committing future changes touching sync code to manual claim/resume testing before release. Two minutes of testing > Sam getting locked out on a Monday morning. Bug Sam hit: PC2's first poll race left state at `true`/`'active'`-equivalent, modal hidden, `enforceTabLock` redirecting every tab activation, no escape from inside the extension — required opening a new Firefox window for a fresh TabFlow tab whose modal hadn't yet wedged.
- **2026-05-13** — Fixed hover-expand "bouncing" reported by Sam: when hovering a truncated workspace name, the sidebar would briefly oscillate between expanded and narrow several times before settling. Root cause was a phantom mouseleave/mouseenter event pair fired by the browser as the CSS transition shifted DOM elements under the cursor — `WorkspaceSidebarItem.handleMouseLeave` called `onWantsHoverCollapse` which immediately set `hoverExpandActive = false`, contracting the sidebar; the cursor (still at the same screen position) then re-entered the now-narrower item, the 150ms timer ran again, and the cycle repeated. Fix in version **0.1.13**: added a `hoverCollapseTimerRef` to `NewTab.tsx` and turned the collapse into a debounced `setTimeout(200ms)`. If `onWantsHoverExpand` fires during the grace window (because the cursor "re-entered" after a phantom leave), the pending collapse is cancelled and the sidebar stays expanded. Intentional leaves still feel snappy — they just settle after 200ms instead of 0ms. No other behavior changes.

- **2026-05-13** — Sidebar workspace labels can now be more descriptive — version **0.1.12** ships three coordinated UX changes for the new-tab sidebar. (1) **Wrap to 2 lines.** `packages/browser-extension/src/newtab/styles.ts` `sidebarWorkspaceName` no longer uses `whiteSpace: 'nowrap'` + `textOverflow: 'ellipsis'` — it now uses `display: '-webkit-box'` + `WebkitLineClamp: 2` + `WebkitBoxOrient: 'vertical'` + `wordBreak: 'break-word'`. A workspace called "3D Printing and Prototyping" now displays as two lines instead of being truncated to "3D Printi…". Each workspace row is ~16px taller. (2) **Drag-to-resize sidebar.** `packages/browser-extension/src/newtab/NewTab.tsx` gained a `sidebarWidth` state initialised to `SIDEBAR_WIDTH` (220), an `isResizingSidebar` state to disable CSS transitions during drag, and a 4px-wide vertical drag handle absolutely positioned on the sidebar's right edge with `cursor: 'ew-resize'`. `mousedown` starts the drag; a window-level `mousemove` listener clamps `e.clientX` to `[180, 400]` and updates the state in real time; `mouseup` ends the drag. The chosen width persists to `chrome.storage.local.tabflow_sidebar_width` and reloads on next open. (3) **Hover-expand on truncated labels.** `packages/browser-extension/src/newtab/WorkspaceSidebarItem.tsx` gained a `nameRef` on the workspace-name span. After 150ms of hover the component lazily checks `scrollHeight > clientHeight + 1` — if the label is being clamped, it fires a new `onWantsHoverExpand` prop. The parent reacts by setting `hoverExpandActive = true`, which widens the sidebar's effective width to `Math.max(sidebarWidth, 380)` with a 200ms CSS transition. `onMouseLeave` cancels any pending timer and collapses back. Sidebars wider than 380px don't expand on hover (the helper only ever widens — never narrows). (4) **Cross-device width sync.** `packages/supabase/tabflow-setup.sql` and the embedded `SETUP_SQL` in `SetupWizard.tsx` gained `ALTER TABLE public.user_settings ADD COLUMN IF NOT EXISTS preferences jsonb NOT NULL DEFAULT '{}'::jsonb;` — a free-form bag for cross-device UI preferences. `SupabaseSyncClient` gained `getPreferences(userId)` and `setPreferences(userId, partial)` methods doing read-modify-write merges into the JSONB column. A new `SAVE_SIDEBAR_WIDTH` message handler in `service-worker.ts` pushes the value to Supabase via `setPreferences`; `initializeSync` pulls preferences once and mirrors `sidebarWidth` to `chrome.storage.local` so the other PC picks up the value on its next sync. Not realtime — eventually consistent on next initializeSync/claim. Existing installs are unaffected: the `preferences` column defaults to `{}` and reads return `{}` until a width is set.
- **2026-05-12** — Parallelized the snapshot push in version **0.1.11** so cross-device sync actually keeps up with rapidly-updating tab URLs (e.g. YouTube's `&t=Ns` time parameter, which the `youtube-time-tracker` content script rewrites every 10s while a video plays). Bug Sam hit: he'd finish a video on PC1, switch to PC2, click "Resume Working Here" — and the video would resume mid-stream, not where he actually left off. Root cause was in `service-worker.ts` `snapshotActiveWorkspace`: after a snapshot wrote new records to IndexedDB, the cloud push iterated `for (const tab of freshTabs) { await syncClient.pushTab(tab, { excludeScroll: true }); }` — sequentially, one HTTP round-trip per tab. With ~500ms per round-trip and 11 tabs in his YouTube workspace, the full push took 5-6 seconds. If Sam closed Firefox or walked away from PC1 inside that window, the most recent `&t=` update never reached Supabase, and PC2's `pullAll` got a stale URL. Fix: replaced the sequential `for await` loop with `await Promise.allSettled(freshTabs.map((tab) => syncClient!.pushTab(tab, { excludeScroll: true })))`. Same Supabase calls, just concurrent — total wall time drops to roughly one round-trip (~500ms) regardless of workspace size. Also parallelized the immediately-following orphan-delete loop the same way (`Promise.allSettled(deletedTabIds.map((id) => syncClient!.deleteTab(id)))`). `allSettled` (not `all`) so one failed row doesn't abort the rest — matches the previous loop's per-row try/catch behavior. Scroll-position pushes are unaffected: those go through their own real-time `SAVE_SCROLL_POSITION` handler (one tab at a time, no batching). The fix can't help with a YouTube tab's `&t=` value that the user changes within ~500ms of closing Firefox, but the practical window where syncs go missing shrinks by ~10×.
- **2026-05-12** — Added explicit Data API `GRANT` statements to the Supabase setup SQL in version **0.1.10**. Supabase emailed all customers that starting May 30, 2026 new projects no longer auto-grant Data API access to tables in the `public` schema (and the same enforcement applies to newly-created tables in existing projects from October 30, 2026). Without explicit `GRANT` statements, supabase-js calls would return 42501 "permission denied" — breaking new-user onboarding entirely. Two coordinated edits: (1) `packages/supabase/tabflow-setup.sql` — the standalone onboarding script — gained a new "Data API grants" section after the RLS policies, granting `SELECT, INSERT, UPDATE, DELETE` on each of the five tables (`user_settings`, `workspaces`, `tabs`, `sessions`, `active_devices`) to `authenticated` and `service_role`. (2) `packages/browser-extension/src/auth/SetupWizard.tsx`'s embedded `SETUP_SQL` constant got the same block (what new users actually copy-paste during the SetupWizard flow). Skipped granting to `anon` — TabFlow authenticates users via Supabase Auth before any Data API call, so `anon` never needs access. Idempotent; existing installs re-running the script are unaffected. Sam's own install is unaffected on either date — his existing tables keep their existing default grants. This change is purely defensive for new signups after May 30.
- **2026-05-12** — Workspace switches no longer leave the main window in a "tabs are hidden until you click a card" state on Firefox. Same family as the 0.1.8 restart fix earlier today: after `moveTabsToHiddenWindow` creates a minimized hidden window and `safeHideMinimizedWindows` runs the native host's `hideMinimized` action, the main window can occasionally end up minimized too — either because Firefox briefly lost focus during the move/restore dance or because of a race in the native host's process walk. The existing safety net called `showWindow(title)` to undo WS_EX_TOOLWINDOW, but `showWindow` doesn't unminimize. Two changes in version **0.1.9**: (1) **`TabManager.safeHideMinimizedWindows`** (`packages/browser-extension/src/background/TabManager.ts`) — the safety-net block now calls `chrome.windows.get(mainWindowId)` first, and if `state === 'minimized'` issues a `chrome.windows.update({ state: 'normal' })` before calling `showWindow`. Works on Chrome and Firefox; doesn't require the native host. (2) **`MessageHandler.handleSwitchWorkspace`** (`packages/browser-extension/src/background/MessageHandler.ts`) — belt-and-suspenders block at the end of the switch (just before the final `saveCurrentTabsToWorkspace`) that does the same `chrome.windows.get` + un-minimize + `focused: true` regardless of whether `safeHideMinimizedWindows` ran. Covers the path where the native host isn't installed (so `safeHideMinimizedWindows` early-returns) and any other future code that could leave the main window minimized after a switch. ACTIVATE_TAB_BY_URL's `focusWindow` self-heal from 0.1.8 was masking this — the user only noticed the bug because the first click on a tile fixed it.
- **2026-05-12** — Firefox restart no longer leaves the active workspace's tabs invisible. Sam reported that closing and reopening Firefox left no pinned TabFlow tab, no workspace tabs at the top of the browser, and clicking tile cards did nothing. Logs proved the restart flow had actually run and restored 11 tabs successfully — into window 853 — but `chrome.windows.getAll()` showed that window 853 was in `state: 'minimized'`. Firefox's session-restore had resurrected the previous session's minimization state; `runChromeRestartFlow` then dumped the workspace tabs into the hidden window without ever raising it. The user saw a separate, newer window (31804) with only the TabFlow newtab they'd opened via `+`, and tile clicks correctly activated tabs in window 853 (cached as `mainWindowId`) — but window 853 was invisible, so activations did nothing visible. Three coordinated changes in version **0.1.8**: (1) **`runChromeRestartFlow` step 1.5** in `packages/browser-extension/src/background/service-worker.ts` — after verifying the TabFlow tab is pinned and capturing `tabFlowWindowId`, force the window to `state: 'normal'` if it's anything other than `'normal' | 'maximized' | 'fullscreen'`, then `focused: true`. Done BEFORE step 3's window cleanup so we never accidentally close the only visible window and leave the user stranded in a hidden one. (2) **`TabManager.ensureMainWindowVisible()`** in `packages/browser-extension/src/background/TabManager.ts` — previously only undid WS_EX_TOOLWINDOW via the native host, which is a no-op if (a) the host isn't installed or (b) the window was never WS_EX_TOOLWINDOW'd in the first place. Now it queries `chrome.windows.get(mainWindowId)` first and calls `chrome.windows.update({ state: 'normal' })` if the window is minimized — works on Chrome and Firefox without the native host. Dropped the early-return `if (!this.nativeHostAvailable) return` since the minimization-undo layer is useful even without the host. (3) **`ACTIVATE_TAB_BY_URL` handler** in `packages/browser-extension/src/background/MessageHandler.ts` — added a `focusWindow(windowId)` helper called after each successful `chrome.tabs.update({ active: true })` (both the exact-match pass and the loose-match pass). Brings the activated tab's window into focus, and un-minimizes it first if needed. This makes card clicks self-heal: a single click on any tile is enough to restore visibility from the stuck state. The existing damage on Sam's install is recoverable two ways — restart Firefox (next session's restart flow will unminimize via change #1), or click any tile in the current TabFlow page (change #3 will restore window 853's visibility). The user's stray window 31804 (with the +-click newtab and about:debugging) can be closed manually after that.
- **2026-04-23** — Picture-in-picture videos now survive workspace switches on Firefox, and the TabFlow version number now appears in the header next to the memory stats. (1) **Firefox PIP fix.** Previously on Firefox, switching workspaces closed the outgoing workspace's tabs (per the 2026-04-16 change that disabled hidden-window mode on Firefox because the native host's `hideMinimized` action only walked `chrome.exe` processes). Closing the source tab destroys the video element, which kills any active PIP. Two coordinated changes restore PIP across switches: (a) `packages/native-host/Program.cs` — `hideMinimized` action now takes an optional `processName` parameter (defaults to `"chrome"` for backward compat with installs that haven't upgraded the host binary). The `HideMinimizedChromeWindows` helper was generalized into `HideMinimizedBrowserWindows(processName)` and `GetChromeProcessIds` into `GetBrowserProcessIds(processName)`; thin shims under the old names preserve internal callers. (b) `packages/browser-extension/src/background/NativeHostClient.ts`'s `hideMinimized()` method gained a typed `processName: 'chrome' | 'firefox' = 'chrome'` parameter and `safeHideMinimizedWindows` in `TabManager.ts` now passes `isFirefox ? 'firefox' : 'chrome'`. (c) `moveTabsToHiddenWindow` and `restoreTabsFromHiddenWindow` no longer early-return on Firefox — both browsers now use hidden minimized windows for inactive workspaces. Net result: switching workspaces on Firefox moves the outgoing workspace's tabs (including the PIP source tab) to a minimized hidden window instead of closing them; PIP keeps playing. The user must rebuild the native host (`dotnet publish -c Release` in `packages/native-host/`) and re-run `install-firefox.bat` to pick up the new `processName` action — without that, hidden windows still work on Firefox but appear in the taskbar (graceful degradation). Chrome behavior is unchanged: the call site sends `processName: 'chrome'`, old hosts ignore the field, new hosts use it. (2) **Version number in header.** `packages/browser-extension/src/newtab/NewTab.tsx` reads `chrome.runtime.getManifest().version` once at module load into a `TABFLOW_VERSION` constant and renders it as a third line in the existing `memoryBlock` next to the System and `<BROWSER>` lines. The block is now always rendered (was previously gated on `systemMemory.total > 0`) so the version line always shows even when the native host isn't installed and memory stats aren't available. Version bumped to 0.1.7.
- **2026-04-23** — Two snapshot-deletion races fixed. After 0.1.5 shipped the event-driven dedupe watchdog for Firefox session restore, Sam reported tabs being silently deleted from workspaces — both during Firefox startup and during history-restore — and that restoring from history had to be repeated several times before it stuck. Two distinct bugs both rooted in the snapshot cleanup sweep running while the browser window was in a transitional state: (a) **runChromeRestartFlow lifted `isStartingUp = false` and `postRestartSnapshotSuppressed = false` BEFORE scheduling the 30-second dedupe watchdog**, so any tab event during the watchdog window (and there are many — session restore is constantly creating tabs) triggered `snapshotActiveWorkspace` → `saveCurrentTabsToWorkspace` → cleanup sweep → deleted DB records for tabs that hadn't yet been re-materialized as browser tabs. Fix: keep both flags `true` for the entire watchdog. The watchdog itself was simplified — it no longer manually re-snapshots after each dedupe pass (was contributing to the race); it just closes duplicate browser tabs. After the 30-second window expires, the listener is removed and the flags are cleared, then normal tab tracking resumes. (b) **`MessageHandler.handleRestoreHistoryEntry` never set `isSwitchingWorkspaces`**, so the `chrome.tabs.remove(...)` calls that close the outgoing tabs each fired `onRemoved` → debounced `snapshotActiveWorkspace` → cleanup sweep → deleted the freshly-saved DB records for the tabs the user JUST asked to restore. Fix: wrapped the entire flow in `this.onSwitchingWorkspacesChange?.(true)` / `(false)` (in a `try/finally` so the flag always lifts even on error), with a 500ms settle after `restoreWorkspaceTabs` so late tab events land while the gate is still up. Net effect: history restore now sticks on the first try; Firefox restart no longer silently deletes workspace tabs. The tension between Sam's "DB is source of truth" principle and the URL-derived tab IDs (which require the cleanup sweep so navigation doesn't accumulate orphan records) is documented but not yet resolved with a bigger refactor.
- **2026-04-23** — Fix for Firefox-only bug where closing and reopening Firefox duplicated every tab in the active workspace. Root cause: Firefox restores its session progressively over several seconds (sometimes much longer on slow machines or large sessions) after the service worker wakes, whereas Chrome's session restore is atomic. `runChromeRestartFlow` in `packages/browser-extension/src/background/service-worker.ts` assumed atomic restore — Step 4 closes the session-restored tabs in the main window, Step 6 re-opens the active workspace's tabs from IndexedDB as `suspended.html` stubs. On Firefox, tabs keep arriving from session restore AFTER Step 6 completes, duplicating the stubs we just recreated (Chrome doesn't hit this because session restore is done before `onStartup` fires). An earlier iteration used a one-shot `setTimeout(..., 4000)` dedupe pass, which wasn't enough — Sam observed duplicates persisting on every Firefox restart because his session restore was taking longer than 4s. Final approach: an event-driven watchdog. After the main flow finishes, a `chrome.tabs.onCreated` listener is attached for 30 seconds. Every new tab that lands in the TabFlow window triggers a 500ms-debounced call to `TabManager.dedupeTabsInWindow(windowId)` — which queries all tabs in the window, groups them by canonical URL (unwrapping `suspended.html?url=...` first via the existing `getRealUrl` helper), and closes all but the first in each group. An initial sweep at 1.5s catches anything already restored before the listener was attached. A final sweep + listener removal fires at the 30s mark. Each dedupe pass re-snapshots the active workspace so IndexedDB and Supabase both reflect the cleaned-up state. Chrome is effectively unaffected — its session restore completes before `onStartup` fires, so the listener times out after 30s having done nothing. Version bumped to 0.1.5 (0.1.4 was already approved on AMO and uploaded to Chrome Web Store).
- **2026-04-20** — Automated release packaging. New `scripts/package-release.mjs` run via `npm run release` (or `npm run package` if `dist/` is already built) produces all four upload zips in one step: `tabflow-chrome-<VERSION>.zip` (dev, with `key` field for unpacked loading), `tabflow-chrome-<VERSION>-store.zip` (Chrome Web Store; `key` stripped from manifest via an in-memory override — no staging directory, so no risk of stale files from prior runs leaking into the archive), `tabflow-firefox-<VERSION>.zip` (AMO), and `tabflow-source-<VERSION>.zip` (AMO's source-code requirement for extensions that ship minified code; excludes `node_modules/`, `dist/`, `.git/`, `bin/`, `obj/`, Vite temp files, sandbox `zi*` leaks, and prior release zips). The version number is read from `manifest.chrome.json` so each release's zips are self-labeled. A stand-alone `BUILD_FROM_SOURCE.md` at the repo root is included in the source zip to give AMO reviewers a self-contained, reviewer-focused build guide. Added `archiver@^7.0.1` to `devDependencies`. `findLiveChunkHashes` scans the entry files for the Rollup-hashed chunks they actually reference so orphan chunks from previous builds are automatically excluded. Script is idempotent and safe to re-run.
- **2026-04-20** — Followup 2: privileged-URL tiles stopped trying to use `suspended.html` as an intermediary, and tile clicks now copy the URL to the clipboard for the unopenable cases. Bug was: after the previous fix preserved `about:debugging` tiles across snapshots, Firefox's workspace-restore after a claim opened the tab as `moz-extension://…/suspended.html?url=about%3Adebugging…`. When the user clicked that suspended shell, `suspended.js` ran `window.location.href = 'about:debugging'` — which Firefox silently blocks (extension pages are forbidden from navigating to `about:*`). Same story for Chrome and `chrome://*`. Sam got a "click anywhere to reload" shell that did nothing. Two changes: (1) `TabManager.restoreWorkspaceTabs` now skips privileged URLs entirely (checked via `crossBrowserOnlyLabel(url) !== null`) — they're no longer pre-opened as suspended tabs. The tile still exists in the TabFlow grid because the pullAll path populated local IndexedDB; clicking the tile goes through `handleOpenTab` which calls `tabs.create` directly (bypassing suspended.html). Log line grew a `(N privileged-URL tiles skipped)` suffix. (2) `TabManager.saveCurrentTabsToWorkspace` preservation broadened: now preserves ANY privileged URL in the cleanup sweep, not just ones inert on the current browser. Matters because e.g. Chrome's post-claim window doesn't have `chrome://extensions/` open (restoreWorkspaceTabs now skips privileged URLs), but the tile must survive the re-snapshot. (3) `NewTab.handleOpenTab` rewritten with three cases: cross-browser privileged URL → copy to clipboard + "Only opens in Chrome/Firefox" toast; Firefox's own `about:*` URL → copy to clipboard + "Paste into address bar" toast (Firefox's `tabs.create` refuses `about:*` even on Firefox itself, so there's no "open it" path); normal URL → activate or create. Every failure path copies the URL to the user's clipboard so pasting into the address bar is one action. Net result: `about:debugging` on Firefox is a tile that, when clicked, copies `about:debugging#/runtime/this-firefox` and tells the user to paste; `chrome://extensions/` on Chrome opens normally via tabs.create; each tile on the wrong browser shows the CHROME ONLY / FIREFOX ONLY chip and also copies its URL. Any stale broken `suspended.html?url=about:debugging` tabs from the previous build need to be closed manually — the next claim cycle won't recreate them. After the initial cross-browser-tile work, Sam found that adding `chrome://extensions/` to a workspace in Chrome and switching to Firefox showed the tile with the CHROME ONLY badge as expected, but switching back to Chrome dropped the tile entirely. Root cause was in `packages/browser-extension/src/background/TabManager.ts` `saveCurrentTabsToWorkspace`: the cleanup sweep deletes any existing storage record whose URL isn't currently open in the browser window. Firefox's window never has a `chrome://extensions/` tab (Firefox can't open chrome://* even through the suspended.html wrapper, or the record ends up being an inert placeholder after claim), so the snapshot after claim would always delete the record. Firefox's subsequent `fullSyncPush` then deleted the row from Supabase, and Chrome's next pull no longer saw it. Fix: in the cleanup sweep, preserve records whose URL fails `isUrlOpenable(url)` — same helper the UI uses to decide whether to render the CHROME ONLY / FIREFOX ONLY badge. A new counter `preservedInert` is included in the "[TabFlow] Saved N tabs" log line so the preservation is visible during debugging. Net effect: `chrome://extensions/` stored by Chrome stays in Firefox's local IndexedDB (as an inert placeholder with a badge), Firefox's `fullSyncPush` upserts it back to Supabase unchanged, and Chrome's subsequent destructive pull re-materializes it correctly in Chrome's workspace. Symmetric for `about:debugging` on Firefox → Chrome.
- **2026-04-20** — Privileged browser URLs (`chrome://*` and `about:*`) can now live as tiles in a workspace. Previously the sync filters in `packages/browser-extension/src/sync/SupabaseSyncClient.ts` (`isSyncableTabUrl`) and `packages/browser-extension/src/background/service-worker.ts` (`isSyncableTab`) dropped anything that wasn't `http(s)://` or `file://`, so e.g. `chrome://extensions/` was captured locally but filtered out before it reached Supabase. Loosened both filters to allow `chrome://*` and `about:*` through (`moz-extension://`, `chrome-extension://`, `edge://`, `vivaldi://`, `javascript:`, `data:`, etc. are still blocked). New helpers in `packages/browser-extension/src/browser-compat.ts`: `isUrlOpenable(url)` returns false when the URL is privileged for the OTHER browser (chrome://* on Firefox, about:* on Chrome; `about:blank` is treated as universal); `crossBrowserOnlyLabel(url)` returns `'Chrome'` / `'Firefox'` / null for UI labels. `handleOpenTab` in `NewTab.tsx` now short-circuits through `isUrlOpenable` and fires a transient toast ("This link only opens in Chrome.") instead of letting `tabs.create` throw an "Illegal URL" error; the tabs.create call itself is now also wrapped in try/catch as belt-and-suspenders. `TabCard.tsx` renders a small `CHROME ONLY` / `FIREFOX ONLY` chip in the top-right of any tile whose URL is inert on the current browser, and dims the card to 65% opacity so it reads as a placeholder at a glance. New styles `crossBrowserBadge` and `openTabToast` in `styles.ts`; `tabCard` gained `position: relative` so the absolute-positioned badge anchors correctly. Net result: Sam can open `chrome://extensions/` in Chrome, TabFlow captures it into the Software Development workspace, it syncs to Firefox, where the tile appears with a "CHROME ONLY" chip and does nothing when clicked except flash a toast. Symmetric for `about:debugging` on Firefox → Chrome. No security exposure: extensions can't run code on privileged pages regardless, and the URLs are encrypted before they hit Supabase.
- **2026-04-20** — Active-device status switched to a database-only model. Bug was: the Resume Working Here modal stopped appearing on Firefox when Chrome held the active claim — the service worker held `_isActiveDevice` in memory and mirrored it to `chrome.storage.session.deviceStatus` and `chrome.storage.local.tabflow_device_active`; the newtab page listened to `chrome.storage.onChanged` for updates. On Firefox, storage-onChanged events from the background service worker do not propagate reliably to extension pages, so when Chrome reclaimed, Firefox's modal stayed hidden. **Fix — the Supabase `active_devices` table is now the single source of truth; every read hits the DB.** Changes: (1) **New `SupabaseSyncClient.isActiveDeviceFromDb()`** method — queries `active_devices` on every call and returns `{ isActive, claimedBy }`. Side effect: refreshes `_isActiveDevice` to match, keeping the internal flag useful for heartbeat/realtime tight loops. Transient errors (network/auth) return `isActive: false` without touching the cache, so a blip can't silently let an inactive device keep pushing. (2) **`GET_DEVICE_STATUS` handler rewritten** in `packages/browser-extension/src/background/service-worker.ts` — always calls `isActiveDeviceFromDb()`. No cache read, no "assume active" default, no fallback to `chrome.storage.local.tabflow_device_active`. Returns `{ success: false, error: 'sync_not_ready' }` if syncClient isn't initialized yet so the UI retries rather than rendering a stale answer. (3) **Newtab polling** in `NewTab.tsx` — the `useEffect` that used to do a one-shot `GET_DEVICE_STATUS` and then listen for `chrome.storage.session.deviceStatus` changes now polls every 500ms until the first success, then settles to every 3s. `chrome.storage.onChanged` is still wired but only for `passphraseMismatch` (which is a one-way latch, not a device-status cache). (4) **Push gates query the DB** — `pushToSync` (service-worker.ts) and the `snapshotActiveWorkspace` push branch both `await syncClient.isActiveDeviceFromDb()` before pushing instead of reading the cached `isActiveDevice` getter. Adds one Supabase round-trip per push, but a device that has quietly lost the claim can no longer push. (5) **`enforceTabLock` simplified** — previously had a fast path reading `syncClient.isActiveDevice`, a slow path reading `chrome.storage.local.tabflow_device_active`, and a Supabase REST fallback. All three replaced with a single `isActiveDeviceFromDb()` call; if syncClient isn't ready, the redirect is skipped (the next tab event after sync initializes will enforce correctly). (6) **`onActiveDeviceChange` callback** (service-worker.ts) no longer writes to `chrome.storage.session.deviceStatus` or `chrome.storage.local.tabflow_device_active` — the UX redirect-to-TabFlow-tab-on-becoming-inactive is the only side effect left. (7) **Realtime handler re-reads the DB** — `SupabaseSyncClient.handleDeviceChange` treats the realtime payload as a "something changed" signal and calls `isActiveDeviceFromDb()` to get the authoritative state. Supabase can deliver cross-table events out of order under load, so trusting `payload.new` is fragile. (8) **`checkActiveDevice` error path** no longer reads `chrome.storage.session.deviceStatus` to recover a "last known" value; transient errors now uniformly default to inactive and fire the `onActiveDeviceChange(false)` callback so the modal appears rather than silently letting the device keep pushing under a stale claim. Both builds clean (background.js 45.46 → 45.33 kB gzipped — slightly smaller, less caching code).
- **2026-04-20** — Memory stats in the top-right header of the new-tab page now work on Firefox, and the Chrome "browser process memory" line actually shows up for the first time (it had been silently broken by a stale native-host name). Changes: (1) **Native host gained two actions** in `packages/native-host/Program.cs` — `get_system_memory` calls Win32 `GlobalMemoryStatusEx` via a new P/Invoke and returns `{ totalBytes, availableBytes }`; `get_browser_memory` takes a `processName` parameter (`"chrome"` or `"firefox"`) and sums `WorkingSet64` across every matching process via `Process.GetProcessesByName()`, returning `{ memoryBytes, processCount }`. Both go through the same length-prefixed JSON stdin/stdout protocol as the existing window-hiding actions. Added `MEMORYSTATUSEX` struct and `kernel32!GlobalMemoryStatusEx` DllImport. (2) **Service worker rewired** in `packages/browser-extension/src/background/service-worker.ts` — the old call used `chrome.runtime.sendNativeMessage('com.tabflow.memory', { action: 'get_chrome_memory' }, ...)` but the native host is registered as `com.tabflow.host` and never implemented `get_chrome_memory`, so the Chrome process line silently never populated. Now it calls `com.tabflow.host` with the new actions via a small `sendNative()` helper that swallows `chrome.runtime.lastError` and returns `null` on failure. On Firefox (where `chrome.system?.memory?.getInfo` doesn't exist), system memory falls back to the host's `get_system_memory` action. On both browsers, process memory comes from `get_browser_memory` with `processName` set to `isFirefox ? 'firefox' : 'chrome'` (imported from `browser-compat.ts`). (3) **UI label per browser** — `packages/browser-extension/src/newtab/NewTab.tsx` gained a module-level `const BROWSER_LABEL = isFirefox ? 'Firefox' : 'Chrome'` and the memory-line now reads `{BROWSER_LABEL} <bytes>/<bytes>`. Tree-shaking leaves only the correct literal in each bundle (verified: `"Chrome"` in `dist/chrome/newtab.js`, `"Firefox"` in `dist/firefox/newtab.js`). (4) **`install-firefox.bat` was already correct** — it writes the Firefox native-host manifest with `allowed_extensions: ["<gecko-id>"]` (not `allowed_origins`) and registers under `HKCU\Software\Mozilla\NativeMessagingHosts`. No change needed. (5) **Tangential vite.config.ts robustness fix** — Vite's `postBuild` plugin copied HTML outputs and the manifest with `cpSync`, which does an unlink+create and fails with `EPERM` on restricted filesystems (e.g. sandboxed mounts or when a file is briefly held by antivirus). Swapped both to `writeFileSync(dest, readFileSync(src))` which rewrites in place. Also wrapped the two `rmSync` cleanups in try/catch so a stray `manifest.<target>.json` leaks rather than aborting the build. Functionally equivalent for normal filesystems; strictly more tolerant otherwise. No Vite behavior change (`emptyOutDir` still true). (6) **Degrades gracefully** — if the native host isn't installed, the calls resolve to `null` via the `sendNative` catch, system memory on Chrome still comes from `chrome.system.memory.getInfo`, and on Firefox the whole memory block is hidden (the UI already conditions on `systemMemory.total > 0`). Users need to rebuild the native host (`dotnet publish -c Release` in `packages/native-host/`) and re-run `install.bat` on Chrome or `install-firefox.bat` on Firefox to pick up the new actions.
- **2026-04-17** — Cross-device scroll position sync (version **0.1.3**). New content script `packages/browser-extension/src/content/scroll-tracker.ts` runs at `document_start` on all URLs. Captures scroll events (debounced 2s), flushes on `visibilitychange` (hidden) and `beforeunload`. Restores scroll on page load and on tab re-activation (visible after >3s hidden) with aggressive retry schedule for lazy-loaded content. User input (wheel/touch/scroll-keys) cancels restore immediately. `history.scrollRestoration = 'manual'` set at document_start to prevent browser native restoration from fighting cross-device scroll. Service worker handlers: `SAVE_SCROLL_POSITION` pushes scroll_x/scroll_y directly to Supabase (skips isActiveDevice gate — scroll updates are safe from any device). `GET_SCROLL_POSITION` queries Supabase directly via `syncClient.getTabScroll(tabId)` — no cache, no IndexedDB. **Critical design decision:** Supabase is the only source of truth for scroll position. Earlier iterations used an in-memory `scrollPositionCache` and IndexedDB as intermediaries, which caused persistent staleness where each browser returned its own last position. All middlemen removed: `scrollPositionCache` deleted from service-worker.ts, `setScrollPositionCache` removed from MessageHandler.ts, scroll cache parameter removed from `TabManager.saveCurrentTabsToWorkspace`. The `pushTab` method gained an `excludeScroll` option — when true, `scroll_x`/`scroll_y` are omitted from the Supabase upsert so batch operations (snapshots, workspace switches, fullSyncPush) don't overwrite individually-pushed values. New `getTabScroll(tabId)` method on `SupabaseSyncClient` for single-row scroll lookups. Registered in both `manifest.chrome.json` and `manifest.firefox.json` as `scroll-tracker.js` at `document_start` matching `<all_urls>`. Both manifests bumped to 0.1.3. Cross-browser sync confirmed working: scroll on Chrome → switch to Firefox → position matches, and vice versa.
- **2026-04-16** — Realtime subscriptions for `workspaces` and `tabs` tables removed from `SupabaseSyncClient.connect()`. Root cause of persistent tab duplication: when Firefox claimed active device and pushed data, Chrome's realtime subscription received tab/workspace events *before* the `active_devices` change notification (Supabase delivers cross-table events in arbitrary order). Chrome still thought it was active → wrote Firefox's tabs into Chrome's IndexedDB → Chrome's next snapshot pushed it all back to the cloud → contamination cycle. The `active_devices` realtime subscription (in `initDeviceSession`) is kept — it only updates `_isActiveDevice` and triggers the Resume modal. All data transfer between browsers now goes exclusively through push (snapshot → pushWorkspace/pushTab) and pull (claim → pullAll destructive + fullSyncPush). Also in this session: (1) `saveCurrentTabsToWorkspace` now returns `{ deletedTabIds }` — the snapshot push deletes those IDs from Supabase immediately, preventing orphan buildup when tabs navigate to new URLs. (2) Hidden window creation disabled on Firefox (`moveTabsToHiddenWindow` and `restoreTabsFromHiddenWindow` return early when `isFirefox`) since the native host that hides minimized windows only targets Chrome processes — Firefox falls back to close+restore-from-suspended.
- **2026-04-16** — Major sync fixes after cross-browser testing exposed three contamination vectors. (1) **Destructive pull on claim** — `pullAll(userId, destructive)` now takes a `destructive` flag. When true (used by `CLAIM_ACTIVE_DEVICE` and `RESTORE_FROM_CLOUD`), it calls `clearLocalData()` first, wiping all local workspaces and tabs so the result is an exact mirror of the cloud with no leftover junk. New `clearLocalData()` method on `SupabaseSyncClient`. (2) **Realtime handler gated behind isActiveDevice** — `handleWorkspaceChange` and `handleTabChange` now return early if `!this._isActiveDevice`. Previously, when Firefox pushed after claiming, Chrome's realtime subscription picked up the changes and wrote them into Chrome's IndexedDB, contaminating Chrome even though it was no longer the active device. (3) **Full sync push with orphan cleanup** — new `fullSyncPush(userId)` method on `SupabaseSyncClient` pushes ALL local workspaces and tabs, then deletes any cloud rows that don't exist locally. Called after a successful claim (step 5 of `claimActiveDeviceWithMaterialization`) and on startup if this device is active (`initializeSync`). This cleans up the hundreds of stale rows from earlier corruption incidents. (4) **Firefox sidebar_action removed** from `manifest.firefox.json` — the sidebar panel showed "Coming Soon" alongside the newtab page, creating a confusing double-sidebar. The newtab page IS the app; the sidebar was vestigial. (5) Previous commit's changes (push-on-snapshot re-enabled, modal stripped down, auto-claim prevention, one-time cleanup removal) remain.
- **2026-04-16** — Cross-browser sync simplified to match the intended mental model: the cloud is always truth, the active browser pushes constantly, any other browser must "Resume Working Here" to pull and recreate. Changes: (1) **Push-on-snapshot re-enabled** in `snapshotActiveWorkspace` — after saving to IndexedDB, the snapshot now pushes the active workspace + its tabs to Supabase, double-gated by `isActiveDevice` and `safeToPush`. This is safe because a second browser that hasn't claimed yet has `safeToPush = false`. (2) **Claim flow simplified** — `claimActiveDeviceWithMaterialization()` no longer takes a mode parameter or offers save-first/close options. It just pulls from cloud, closes all tabs, opens the workspace's tabs, re-snapshots, claims, enables push. Removed `saveMainWindowToRecoveredWorkspace` and `verifyWindowMatchesWorkspace` helpers (no more Option C, no more verification). (3) **Modal stripped down** — the Resume Working Here modal is now just a title, subtitle, body text, and a single button. No radio options, no tab count, no choices. Removed `claimTabCount`, `claimMode` state and `GET_MAIN_WINDOW_TABS_SUMMARY` message handler. Removed `resumeModalChoices/Choice/ChoiceLabel/ChoiceHint` styles. (4) **Second browser no longer auto-claims** — `checkActiveDevice()` in `SupabaseSyncClient.ts` will only auto-claim if no `active_devices` row exists or if this device's own row is found. It will NOT auto-claim over another device's stale heartbeat; the user must explicitly click Resume Working Here. This prevents Firefox from silently stealing the claim on load and locking Chrome out. (5) **One-time cleanup block removed** from service worker startup — the `dbCleanedV2`-gated emergency cleanup (which closes all windows + wipes all tabs) was left over from an earlier corruption fix and would fire on any fresh install (including Firefox), nuking the browser. It already ran on Chrome so removing it is safe. Both builds clean (background.js 45.34 → 44.16 kB gzipped, newtab.js 15.76 → 15.32 kB gzipped).
- **2026-04-15** — Cross-browser sync Phase 4 redesigned after the first-cut re-enable from earlier today corrupted Chrome. Root cause: `snapshotActiveWorkspace` assumes the browser window's open tabs faithfully represent the active workspace. That's true on a single browser the user has been actively using, but it's NOT true on a second browser that just pulled a workspace from the cloud — Firefox's window still held about:debugging, the newtab itself, random pages; the snapshot wrote those into the active workspace, pushed it up, and Chrome's realtime subscription mirrored the junk back down. Four changes in `packages/browser-extension/src/background/service-worker.ts`: (1) **Push-on-snapshot permanently disabled** in `snapshotActiveWorkspace`. Explicit user-intent actions (create / rename / delete / color / switch / move tabs) still push through `pushToSync`; snapshots never do. Comment at the old push site now explains why. (2) **`safeToPush` gate** added as a second condition (on top of `isActiveDevice`) for every push. Stored as `tabflow_safe_to_push` in `chrome.storage.local`, checked at the top of `pushToSync`. Set to `true` only after a successful claim-with-materialization or `RESTORE_FROM_CLOUD`, OR auto-migrated to `true` for existing installs that already have local workspaces (so current Chrome user doesn't get sync-silent after updating). Fresh installs start with the flag unset and can't push until they properly claim. Migration runs ~1.5s after service worker startup. (3) **`CLAIM_ACTIVE_DEVICE` rewritten** to take a `{ mode: 'close' | 'save-first' }` payload and route through a new `claimActiveDeviceWithMaterialization()` function. Flow: `setPushing(true)` → `pullAll` (additive upsert) → `isSwitchingWorkspaces = true` → optionally `saveMainWindowToRecoveredWorkspace` if mode is save-first → `closeAllTabs` → `restoreWorkspaceTabs` for the active workspace → 800ms settle → re-snapshot → `verifyWindowMatchesWorkspace` (canonicalized URL multiset comparison) → if diverged, Option C fallback auto-saves current window tabs to a Recovered workspace then aborts with a diagnostic error → otherwise `claimActiveDevice()` + `markSafeToPush`. `isSwitchingWorkspaces` stays true throughout so snapshots can't race the reshuffle. Crypto failures on pull abort with a passphrase-mismatch message; pull network errors also abort (we refuse to materialize against an incomplete cloud state). (4) **`GET_MAIN_WINDOW_TABS_SUMMARY` message** added, returns `{ count, urls }` of the current main-window non-TabFlow tabs so the pre-claim modal can show "this will close 14 tabs". Two helper functions added: `saveMainWindowToRecoveredWorkspace(mainWindowId, context)` creates an inactive "Recovered from {BROWSER} {YYYY-MM-DD HH:mm}" workspace and snapshots the window into it (used by both save-first mode and Option C); `verifyWindowMatchesWorkspace(mainWindowId, workspaceId)` compares canonicalized URL multisets, unwrapping `suspended.html?url=` wrappers. UI changes in `packages/browser-extension/src/newtab/NewTab.tsx`: the Resume Working Here **banner** across the top was replaced with a centered blocking **modal** (full-viewport `rgba(5,8,16,0.72)` backdrop with `backdrop-filter: blur(4px)`, dark centered card with a blue accent gradient at the top, z-index 9999) that the user cannot dismiss without claiming — this is because claim is destructive (replaces the browser's tabs) and needs to be an informed choice. Modal has two radio-button options: "Save current tabs to a new workspace first" (default, recommended) and "Close them (they're saved elsewhere)". Shows a live count from `GET_MAIN_WINDOW_TABS_SUMMARY`. On failure, surfaces the background's error message inline. New state in `NewTab.tsx`: `claimTabCount`, `claimMode`, `claimInProgress`, `claimError`. All modal styles added to `packages/browser-extension/src/newtab/styles.ts` under `resumeModal*` keys. Both Chrome and Firefox builds clean (background.js gzipped grew 44.29 → 45.34 kB for the materialization helpers; newtab.js grew 14.95 → 15.76 kB for the modal). Needs a fresh cross-browser test — this time Firefox's claim flow should pull Chrome's data, close Firefox's current tabs, open the workspace's tabs, verify, then claim. Sam's existing Chrome install is safe (migration sets `safeToPush = true` on startup since Chrome has workspaces).
- **2026-04-15** — Cross-browser sync Phase 4 re-enabled. Two call sites in `packages/browser-extension/src/background/service-worker.ts` changed: (1) `snapshotActiveWorkspace` now pushes to Supabase after every debounced snapshot, gated by `syncClient.isActiveDevice` and wrapped in `setPushing(true)/setPushing(false)` to suppress the realtime echo guard. Pushes the workspace row plus all its tabs, filtered through the existing `isSyncableTab` / `isSyncableFavicon` helpers so `chrome://`, `about:`, `moz-extension://`, and non-`data:` / non-`http(s):` favicon URLs never hit the cloud. (2) `CLAIM_ACTIVE_DEVICE` now calls `syncClient.pullAll(syncUserId)` before `claimActiveDevice()`, wrapped in the same `setPushing` guard. The pull is additive — `pullAll` → `saveWorkspace` / `saveTab` are upsert-by-id, so local rows not present in the cloud are preserved, and a mid-pull failure leaves the previous local state intact plus whatever rows came in cleanly. Crypto failures (`DOMException` / `OperationError`) abort the claim and surface a passphrase-mismatch error to the UI; non-crypto pull errors log a warning and the claim still proceeds so the user isn't blocked from taking over. The decision to go additive (not clear-then-pull) is because the April 15 incident showed that clear-then-pull has no safety net — if the pull is wrong or partial, local state is already gone. Also updated `initializeSync()` comment at line 299 from "DISABLED — push-only mode" to "pull on claim / restore only" to reflect the new model. This change is only safe because deterministic tab IDs (`tab-<16hex>` content hashes from the Phase 1–3 migration earlier today) make Chrome and Firefox produce the same ID for the same URL in the same workspace, so realtime echo and `pullAll` upserts can't corrupt the cloud the way they did on the morning of April 15. Both Chrome and Firefox builds clean at identical sizes (background.js gzipped: 44.29 kB for both). Pre-existing typecheck errors in the codebase (string-literal MessageType comparisons, SupabaseSyncClient payload casts, config.ts nullability) are unrelated and not touched.
- **2026-04-15** — HistoryPanel and ArchivePanel extracted from `NewTab.tsx`. New siblings `packages/browser-extension/src/newtab/HistoryPanel.tsx` (163 lines) and `ArchivePanel.tsx` (135 lines). NewTab.tsx dropped from 1,684 → 1,485 lines (-12%). Both components are pure presentational — fetching, mutation, and panel-open state all stay in NewTab. HistoryPanel takes 9 props: `loading`, `entries`, `index`, `setIndex`, `confirmRestore`, `setConfirmRestore`, `restoring`, `onRestore`, `onClose`. It's defined with `React.forwardRef` so the parent can still attach `historyPanelRef` to the root `<div>` (the ref is currently unused at the read end, but preserving the DOM wiring in case it's needed later). `formatTimeAgo` moved into `HistoryPanel.tsx` as a module-local helper — it was only used by the history UI. ArchivePanel takes 6 props: `loading`, `deletedWorkspaces`, `selectedIds`, `onToggleSelection`, `onRestore`, `onPermanentlyDelete`. While here, the `deletedWorkspaces` state in NewTab was retyped from `any[]` to `DeletedWorkspace[]` (the type already existed in `@tabflow/core`). Chrome and Firefox builds clean; newtab.js gzipped grew modestly (14.62 → 14.95 kB) from the extra module boilerplate — standard cost for a split.
- **2026-04-15** — Dragged tab tile halo switched to workspace accent color in `packages/browser-extension/src/newtab/NewTab.tsx`. The previous fix kept the halo amber (`rgba(255, 200, 120, ...)` etc.) — a leftover from the original zero-blur amber ring. Result: press-and-hold showed the workspace-color glow from TabCard, but the moment drag started the halo shifted to warm yellow, which read as a distracting color change rather than a continuation of the pressed state. Replaced the three amber layers with `${dragAccent}80 / 4d / 26` where `dragAccent = activeWorkspace?.color || '#6c8cff'` (same fallback TabCard uses). Cast shadow unchanged. Halo is now visually continuous from press → drag in the workspace's own color.
- **2026-04-15** — Hard-edge amber outline on dragged tab tiles fixed in `packages/browser-extension/src/newtab/NewTab.tsx`. After the TabCard inset-glow fix from earlier today, press-and-hold on a tile looked right — but the moment the cursor moved past the drag threshold and the floating drag tile appeared, a crisp cold edge reappeared around the tile. Root cause: the floating drag tile's wrapper `boxShadow` led with `0 0 0 2px rgba(255, 200, 120, 0.45)` — a zero-blur, 2px-spread amber ring. That's a literal sharp outline around the card. Replaced the ring with three blurred amber halo layers (15px @ 50% + 35px @ 30% + 70px @ 15%) stacked with the existing dark cast shadow (promoted from `0 6px 20px` to a unified `0 10px 30px` to match TabCard's pressed state). Same "this is being dragged" visual cue, no hard boundary. Both builds clean.
- **2026-04-15** — Hard-edge backlit glow on tab tiles fixed in `packages/browser-extension/src/newtab/TabCard.tsx`. Previously the exterior `box-shadow` halo emanated at `accentColor50` (~31% alpha) right at the card's rectangular boundary, while the card's interior stayed flat `#24272f` — that contrast step (bright halo just outside / dark flat inside) read as a visible hard edge wrapping the tile. Fix: layered matching `inset` box-shadows into both hover and pressed states so the card's interior edge brightens to meet the exterior halo continuously. Hover gets `inset 0 0 15px 0px ${accentColor}20` (one layer, paired with the existing 12/25px exterior blurs). Pressed gets two layers — `inset 0 0 25px 0px ${accentColor}35` + `inset 0 0 50px 0px ${accentColor}18` — stacked in front of the existing 5-layer exterior halo. No transform, color, or cast-shadow changes; the lift and brightness filter are untouched. Removed the "hard edge on backlit glow" item from §9 known issues. Both builds clean (newtab.js 14.59 kB → 14.62 kB gzipped — +30 bytes for the extra shadow layers).
- **2026-04-15** — `paddingX` / `paddingY` / `marginX` / `marginY` typos fixed in `packages/browser-extension/src/newtab/styles.ts`. These were pre-existing invalid CSSProperties fields that React silently dropped at runtime — `workspacesList` had `paddingX: '8px'` + `paddingY: 0` and `newWorkspaceForm` had `marginX: '8px'` + `marginY: '8px'`. Translated to valid CSS shorthand (`padding: '0 8px'` and `margin: '8px'` respectively) so the author's original intent now actually applies. This is a small visible layout change: the workspace list in the sidebar now has 8px of horizontal padding, and the new-workspace form has 8px of margin around it. Also dropped the file-header comment that flagged them as TODO and the `as React.CSSProperties` casts (no longer needed now that the fields are valid). Both builds clean (newtab.js gzipped: 14.61 kB → 14.59 kB).
- **2026-04-15** — Dead `recentlyRemovedTabs` code removed from `packages/browser-extension/src/background/service-worker.ts`. The Set was a write-only leftover from the pre-deterministic-ID era — three call sites added `chrome-<id>` keys with 5–10s self-expiring timers, but nothing ever read the Set. Removed the declaration, the two write sites (the `handleMoveTabsInServiceWorker` loop and the `chrome.tabs.onRemoved` handler entry), and updated the two stale comments that still referenced it (`service-worker.ts` near the MOVE_TABS dispatch, and `MessageHandler.ts` L497 on the move-tabs return). No behavior change — just removes a tiny amount of cruft and the attendant `setTimeout` timers. Both Chrome and Firefox builds clean at identical gzipped sizes.
- **2026-04-15** — `NewTab.tsx` split into smaller files. The new-tab page was a 3,278-line monster holding the orchestrator, two sub-components, and a 1,162-line styles blob in a single file. Extracted four siblings into `packages/browser-extension/src/newtab/`: `constants.ts` (SIDEBAR_WIDTH, COLOR_PALETTE, formatBytes), `styles.ts` (the full styles object, unchanged apart from being moved), `WorkspaceSidebarItem.tsx` (the sidebar row component with its rename/color/short-name context menu), and `TabCard.tsx` (the tile with the backlit-glow hover/press effect). `NewTab.tsx` is now 1,675 lines and imports from the new modules. Props, render output, and behavior are byte-for-byte identical — this was a pure move, not a rewrite. Pre-existing `paddingX`/`paddingY`/`marginX`/`marginY` typos in the styles object (which React silently drops) were preserved verbatim with a comment in `styles.ts` flagging them for a future pass. No schema changes, no new dependencies. Both Chrome and Firefox builds produce identical bundles (14.49 kB → 14.61 kB gzipped newtab.js — a few hundred bytes of extra module boilerplate). HistoryPanel and ArchivePanel stay inline because they're threaded deep into NewTab's state; extracting them would require props plumbing that's better saved for a follow-up pass.
- **2026-04-15** — `shortName` revert bug fixed in `packages/browser-extension/src/sync/SupabaseSyncClient.ts`. Manually-set workspace short-names (e.g. "GNS" on the Guns workspace, overriding the default "GU") were reverting to the auto-generated letters whenever any sync push fired. Root cause: `pushWorkspace` only uploads the columns the Supabase `workspaces` schema has (`id`, `name`, `color`, `icon`, `sort_order`, `is_active`, `version`, `updated_at`) — `shortName` is a local-only field the cloud schema doesn't track. When the realtime subscription echoed that push back, or when `pullAll()` ran, the incoming payload had no `shortName`, so `handleWorkspaceChange` / `pullAll` reconstructed the `Workspace` object with `shortName: undefined` and `saveWorkspace` clobbered the local row. Fix: both paths now read the existing local workspace first (`await this.storage.getWorkspace(id)`) and merge `shortName: existing?.shortName` into the object before saving, so remote echoes can't wipe the local-only field. No schema change required — solving it in the cloud would mean adding a column and encrypting shortName, which is overkill for a 3-character UI label.
- **2026-04-15** — Deterministic tab ID migration (Phases 1–3). Replaced the old `chrome-<numericId>` tab ID scheme — which embedded Chrome's runtime tab ID and caused the Chrome/Firefox collision documented in §9 — with content-derived IDs of the form `tab-<16hex>`, where the hex is the first 16 chars of `sha256(workspaceId | canonicalUrl | firstSeenAt)`. Canonical URLs strip the trailing slash and lowercase the host. New `packages/browser-extension/src/utils/tabId.ts` exports `computeTabId()` and `canonicalizeUrl()`. Since the storage ID no longer carries the Chrome tab ID, we can't do `chrome.tabs.update(storageId)` to activate a tile — instead, `TabManager.buildMainWindowUrlIndex()` returns a `Map<canonicalUrl, chromeTabId[]>` built by querying `chrome.tabs.query({ windowId })`, unwrapping the `suspended.html?url=` wrapper when needed, and excluding the TabFlow tab itself. Handlers that used to regex `storageId.match(/^chrome-(\d+)$/)` to recover the Chrome tab ID now look it up by canonical URL instead: `handleOpenTab` (UI-side, routes through `ACTIVATE_TAB_BY_URL`), `handleRemoveTab`, `handleCloseAllTabs`, `handleMoveTabs`, `handleReorderTabs` in `MessageHandler.ts`, and `handleMoveTabsInServiceWorker` in `service-worker.ts`. Duplicate URLs are handled with `urlIndex.get(canonical)?.shift()` so the same Chrome ID isn't consumed twice. Phase 4 (re-enable cross-browser sync push/pull now that IDs are deterministic) is deliberately deferred — infrastructure is in place, but we're leaving push-only / pull-disabled until we've stress-tested the new IDs locally. `recentlyRemovedTabs.add(\`chrome-${id}\`)` remains at three spots in `service-worker.ts` but is dead code (the Set is never read) — left untouched to minimize diff.
- **2026-04-15** — Cross-browser sync partially disabled after a full-day debugging session exposed a fundamental tab-ID collision bug between Chrome and Firefox. Timeline: (1) Added `chrome-extension://` → `getExtensionBaseUrl()` / `getExtensionPageUrl()` wrappers across `service-worker.ts`, `TabManager.ts`, `MessageHandler.ts` (new helpers in `browser-compat.ts`) so Firefox stops throwing "address wasn't understood" on `chrome-extension://tabflow@samhamilton.dev/newtab.html`. Also added `moz-extension://` to the `isCapturable` URL filter. Firefox manifest: changed `background.service_worker` → `background.scripts` since FF 149's MV3 rejects the service_worker key. These are all permanent and good. (2) Added aggressive sync behavior that turned out to be dangerous: `CLAIM_ACTIVE_DEVICE` was rewritten to clear all local tabs and then `pullAll()` from Supabase before claiming, and `snapshotActiveWorkspace` began pushing every tab change to Supabase. These were intended to make "Resume Working Here" pull-not-push and to make day-to-day edits sync across browsers. In practice both paths amplified a pre-existing ID collision: tab IDs are generated as `chrome-<numericId>` where `numericId` is the browser's internal `chrome.tabs` ID — Chrome and Firefox each start counting from 1, so unrelated tabs in each browser map to the same Supabase row. Upserts from Firefox overwrote `workspace_id` on rows Chrome had pushed, causing tabs to visibly teleport between workspaces. Compounding: `saveCurrentTabsToWorkspace` regenerates IDs on every snapshot, so the cloud accumulated hundreds of zombie rows (368 in Sam's case) that `pullAll` mass-imported as duplicates, and the realtime echo of all the churn caused UI thrash (multiple workspaces highlighted at once, state flicker). (3) Rollback (current state): `CLAIM_ACTIVE_DEVICE` is back to a pure `claimActiveDevice()` — no clear, no pull. `snapshotActiveWorkspace` no longer pushes to Supabase at all. Explicit actions (create/rename/delete workspace) still push as they always have. Kept as harmless defensive additions: URL / favicon filters in `SupabaseSyncClient.pullAll` and `handleTabChange` (drop `chrome://…`, `about:…`, `moz-extension://…` on inbound, drop non-`data:` / non-`http(s):` favicons like `chrome://global/skin/icons/info.svg`), and a `replaceWorkspaceTabs` method that's defined but currently unused (kept for the eventual redesign). Sam wiped Supabase `tabs` + `workspaces` tables and the local `TabFlowDB` IndexedDB, then rebuilt workspaces by hand. Firefox is not to be opened against this install until cross-browser sync is redesigned. See §9 for the real fix (deterministic IDs).
- **2026-04-13** — Stale `tabFlowTabId` collision fix in `packages/browser-extension/src/background/service-worker.ts` (`ensureTabFlowTab`, Strategy 3). After a full Chrome restart, Chrome reassigns every tab a fresh numeric ID, so the `tabFlowTabId` persisted in `chrome.storage.local` from the previous session can now coincidentally point to a completely unrelated tab — notably `chrome://extensions`, which is commonly open during unpacked-install development when the extension reloads. The old Strategy 3 matched purely on ID and handed the tab straight to the pin/move logic, which would then pin `chrome://extensions` at index 0 and never route to the real TabFlow newtab. Strategies 1/2/4/5 didn't save us because on a cold service-worker wake-up the TabFlow tab often hasn't been re-created yet. Fix: Strategy 3 now validates the candidate tab's URL before trusting the stored ID — accepts only `chrome-extension://<extensionId>/…` (URL or `pendingUrl`), `chrome://newtab/` / `chrome://newtab`, empty string, or `about:blank`, and explicitly rejects `suspendedPrefix`. If the candidate doesn't match, the stale ID is removed from `chrome.storage.local` so subsequent runs don't keep hitting the same collision, and the function falls through to Strategies 4/5 which correctly locate (or create) the real keeper. No schema changes, no behavior change on the happy path.
- **2026-04-13** — Data-loss safeguards added after the folder-rename incident permanently destroyed a user's local workspaces (new extension ID → empty IndexedDB; cloud rows present but undecryptable because the encryption salt was only ever stored in chrome.storage.local and had been GC'd by Chrome). Three defenses now ship together in version **0.1.2**:
  1. **Pinned extension ID.** `packages/browser-extension/public/manif